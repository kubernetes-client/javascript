# Function: loadYaml()

> **loadYaml**\<`T`\>(`data`, `opts?`): `T`

Defined in: [src/yaml.ts:12](https://github.com/davidgamero/javascript/blob/019ff89c9f584a4d3b0d6eda91fb6ec8395636c9/src/yaml.ts#L12)

Load a Kubernetes object from YAML.

## Type Parameters

### T

`T`

## Parameters

### data

`string`

The YAML string to load.

### opts?

`any`

Optional YAML load options.

## Returns

`T`

The deserialized Kubernetes object.
