# Function: dumpYaml()

> **dumpYaml**(`object`, `opts?`): `string`

Defined in: [src/yaml.ts:43](https://github.com/davidgamero/javascript/blob/019ff89c9f584a4d3b0d6eda91fb6ec8395636c9/src/yaml.ts#L43)

Dump a Kubernetes object to YAML.

## Parameters

### object

`any`

The Kubernetes object to dump.

### opts?

`any`

Optional YAML dump options.

## Returns

`string`

The YAML string representation of the serialized Kubernetes object.
