# Function: dumpYaml()

> **dumpYaml**(`object`, `opts?`): `string`

Defined in: [src/yaml.ts:43](https://github.com/davidgamero/javascript/blob/e0fd9c7e722ede8cfc1a9d68a223a359233058ff/src/yaml.ts#L43)

Dump a Kubernetes object to YAML.

## Parameters

### object

`any`

The Kubernetes object to dump.

### opts?

`any`

Optional YAML dump options.

## Returns

`string`

The YAML string representation of the serialized Kubernetes object.
