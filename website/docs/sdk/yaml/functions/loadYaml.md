# Function: loadYaml()

> **loadYaml**\<`T`\>(`data`, `opts?`): `T`

Defined in: [src/yaml.ts:12](https://github.com/davidgamero/javascript/blob/e0fd9c7e722ede8cfc1a9d68a223a359233058ff/src/yaml.ts#L12)

Load a Kubernetes object from YAML.

## Type Parameters

### T

`T`

## Parameters

### data

`string`

The YAML string to load.

### opts?

`any`

Optional YAML load options.

## Returns

`T`

The deserialized Kubernetes object.
