// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v3.21.12
// source: k8s.io/api/extensions/v1beta1/generated.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import {
    LabelSelector,
    ListMeta,
    ObjectMeta,
    Time,
} from '../../../apimachinery/pkg/apis/meta/v1/generated.js';
import { IntOrString } from '../../../apimachinery/pkg/util/intstr/generated.js';
import { PodTemplateSpec, TypedLocalObjectReference } from '../../core/v1/generated.js';

/**
 * DEPRECATED - This group version of DaemonSet is deprecated by apps/v1beta2/DaemonSet. See the release notes for
 * more information.
 * DaemonSet represents the configuration of a daemon set.
 * +k8s:supportsSubresource="/status"
 */
export interface DaemonSet {
    /**
     * metadata is the standard object metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * spec is the desired behavior of this daemon set.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * +optional
     */
    spec?: DaemonSetSpec | undefined;
    /**
     * status is the current status of this daemon set. This data may be
     * out of date by some window of time.
     * Populated by the system.
     * Read-only.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * +optional
     */
    status?: DaemonSetStatus | undefined;
}

/** DaemonSetCondition describes the state of a DaemonSet at a certain point. */
export interface DaemonSetCondition {
    /** type is the type of DaemonSet condition. */
    type?: string | undefined;
    /** status is the status of the condition, one of True, False, Unknown. */
    status?: string | undefined;
    /**
     * lastTransitionTime is the last time the condition transitioned from one status to another.
     * +optional
     */
    lastTransitionTime?: Time | undefined;
    /**
     * reason is the reason for the condition's last transition.
     * +optional
     */
    reason?: string | undefined;
    /**
     * message is a human readable message indicating details about the transition.
     * +optional
     */
    message?: string | undefined;
}

/** DaemonSetList is a collection of daemon sets. */
export interface DaemonSetList {
    /**
     * Standard list metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ListMeta | undefined;
    /** A list of daemon sets. */
    items: DaemonSet[];
}

/** DaemonSetSpec is the specification of a daemon set. */
export interface DaemonSetSpec {
    /**
     * selector is a label query over pods that are managed by the daemon set.
     * Must match in order to be controlled.
     * If empty, defaulted to labels on Pod template.
     * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
     * +optional
     */
    selector?: LabelSelector | undefined;
    /**
     * template is an object that describes the pod that will be created.
     * The DaemonSet will create exactly one copy of this pod on every node
     * that matches the template's node selector (or on every node if no node
     * selector is specified).
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#pod-template
     */
    template?: PodTemplateSpec | undefined;
    /**
     * updateStrategy is an update strategy to replace existing DaemonSet pods with new pods.
     * +optional
     */
    updateStrategy?: DaemonSetUpdateStrategy | undefined;
    /**
     * minReadySeconds is the minimum number of seconds for which a newly created DaemonSet pod should
     * be ready without any of its container crashing, for it to be considered
     * available. Defaults to 0 (pod will be considered available as soon as it
     * is ready).
     * +optional
     */
    minReadySeconds?: number | undefined;
    /**
     * templateGeneration is a sequence number representing a specific generation of the template, it is DEPRECATED.
     * Populated by the system. It can be set only during the creation.
     * +optional
     */
    templateGeneration?: number | undefined;
    /**
     * revisionHistoryLimit is the number of old history to retain to allow rollback.
     * This is a pointer to distinguish between explicit zero and not specified.
     * Defaults to 10.
     * +optional
     */
    revisionHistoryLimit?: number | undefined;
}

/** DaemonSetStatus represents the current status of a daemon set. */
export interface DaemonSetStatus {
    /**
     * The number of nodes that are running at least 1
     * daemon pod and are supposed to run the daemon pod.
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/
     */
    currentNumberScheduled?: number | undefined;
    /**
     * The number of nodes that are running the daemon pod, but are
     * not supposed to run the daemon pod.
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/
     */
    numberMisscheduled?: number | undefined;
    /**
     * The total number of nodes that should be running the daemon
     * pod (including nodes correctly running the daemon pod).
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/
     */
    desiredNumberScheduled?: number | undefined;
    /**
     * The number of nodes that should be running the daemon pod and have one
     * or more of the daemon pod running and ready.
     */
    numberReady?: number | undefined;
    /**
     * The most recent generation observed by the daemon set controller.
     * +optional
     */
    observedGeneration?: number | undefined;
    /**
     * The total number of nodes that are running updated daemon pod
     * +optional
     */
    updatedNumberScheduled?: number | undefined;
    /**
     * The number of nodes that should be running the
     * daemon pod and have one or more of the daemon pod running and
     * available (ready for at least spec.minReadySeconds)
     * +optional
     */
    numberAvailable?: number | undefined;
    /**
     * The number of nodes that should be running the
     * daemon pod and have none of the daemon pod running and available
     * (ready for at least spec.minReadySeconds)
     * +optional
     */
    numberUnavailable?: number | undefined;
    /**
     * Count of hash collisions for the DaemonSet. The DaemonSet controller
     * uses this field as a collision avoidance mechanism when it needs to
     * create the name for the newest ControllerRevision.
     * +optional
     */
    collisionCount?: number | undefined;
    /**
     * Represents the latest available observations of a DaemonSet's current state.
     * +optional
     * +patchMergeKey=type
     * +patchStrategy=merge
     * +listType=map
     * +listMapKey=type
     */
    conditions: DaemonSetCondition[];
}

/**
 * DaemonSetUpdateStrategy indicates the strategy that the DaemonSet
 * controller will use to perform updates. It includes any additional parameters
 * necessary to perform the update for the indicated strategy.
 */
export interface DaemonSetUpdateStrategy {
    /**
     * type is the type of daemon set update. Can be "RollingUpdate" or "OnDelete".
     * Default is OnDelete.
     * +optional
     */
    type?: string | undefined;
    /**
     * rollingUpdate is the rolling update config params. Present only if type = "RollingUpdate".
     * ---
     * TODO: Update this to follow our convention for oneOf, whatever we decide it
     * to be. Same as Deployment `strategy.rollingUpdate`.
     * See https://github.com/kubernetes/kubernetes/issues/35345
     * +optional
     */
    rollingUpdate?: RollingUpdateDaemonSet | undefined;
}

/**
 * DEPRECATED - This group version of Deployment is deprecated by apps/v1beta2/Deployment. See the release notes for
 * more information.
 * Deployment enables declarative updates for Pods and ReplicaSets.
 * +k8s:supportsSubresource="/status"
 */
export interface Deployment {
    /**
     * metadata is the standard object metadata.
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * spec is the specification of the desired behavior of the Deployment.
     * +optional
     */
    spec?: DeploymentSpec | undefined;
    /**
     * status is the most recently observed status of the Deployment.
     * +optional
     */
    status?: DeploymentStatus | undefined;
}

/** DeploymentCondition describes the state of a deployment at a certain point. */
export interface DeploymentCondition {
    /** type is the type of deployment condition. */
    type?: string | undefined;
    /** status is the status of the condition, one of True, False, Unknown. */
    status?: string | undefined;
    /** lastUpdateTime is the last time this condition was updated. */
    lastUpdateTime?: Time | undefined;
    /** lastTransitionTime is the last time the condition transitioned from one status to another. */
    lastTransitionTime?: Time | undefined;
    /** reason is the reason for the condition's last transition. */
    reason?: string | undefined;
    /** message is a human readable message indicating details about the transition. */
    message?: string | undefined;
}

/** DeploymentList is a list of Deployments. */
export interface DeploymentList {
    /**
     * Standard list metadata.
     * +optional
     */
    metadata?: ListMeta | undefined;
    /** Items is the list of Deployments. */
    items: Deployment[];
}

/**
 * DEPRECATED.
 * DeploymentRollback stores the information required to rollback a deployment.
 */
export interface DeploymentRollback {
    /** name is required: This must match the Name of a deployment. */
    name?: string | undefined;
    /**
     * updatedAnnotations are the annotations to be updated to a deployment
     * +optional
     */
    updatedAnnotations: { [key: string]: string };
    /** rollbackTo is the config of this deployment rollback. */
    rollbackTo?: RollbackConfig | undefined;
}

export interface DeploymentRollback_UpdatedAnnotationsEntry {
    key: string;
    value: string;
}

/** DeploymentSpec is the specification of the desired behavior of the Deployment. */
export interface DeploymentSpec {
    /**
     * replicas is the number of desired pods. This is a pointer to distinguish between explicit
     * zero and not specified. Defaults to 1.
     * +optional
     */
    replicas?: number | undefined;
    /**
     * selector is the label selector for pods. Existing ReplicaSets whose pods are
     * selected by this will be the ones affected by this deployment.
     * +optional
     */
    selector?: LabelSelector | undefined;
    /** template describes the pods that will be created. */
    template?: PodTemplateSpec | undefined;
    /**
     * strategy is the deployment strategy to use to replace existing pods with new ones.
     * +optional
     * +patchStrategy=retainKeys
     */
    strategy?: DeploymentStrategy | undefined;
    /**
     * minReadySeconds is the minimum number of seconds for which a newly created pod should be ready
     * without any of its container crashing, for it to be considered available.
     * Defaults to 0 (pod will be considered available as soon as it is ready)
     * +optional
     */
    minReadySeconds?: number | undefined;
    /**
     * revisionHistoryLimit is the number of old ReplicaSets to retain to allow rollback.
     * This is a pointer to distinguish between explicit zero and not specified.
     * This is set to the max value of int32 (i.e. 2147483647) by default, which
     * means "retaining all old ReplicaSets".
     * +optional
     */
    revisionHistoryLimit?: number | undefined;
    /**
     * paused indicates that the deployment is paused and will not be processed by the
     * deployment controller.
     * +optional
     */
    paused?: boolean | undefined;
    /**
     * rollbackTo is the config this deployment is rolling back to. Will be cleared after rollback is done. it is DEPRECATED.
     * +optional
     */
    rollbackTo?: RollbackConfig | undefined;
    /**
     * progressDeadlineSeconds is the maximum time in seconds for a deployment to make progress before it
     * is considered to be failed. The deployment controller will continue to
     * process failed deployments and a condition with a ProgressDeadlineExceeded
     * reason will be surfaced in the deployment status. Note that progress will
     * not be estimated during the time a deployment is paused. This is set to
     * the max value of int32 (i.e. 2147483647) by default, which means "no deadline".
     * +optional
     */
    progressDeadlineSeconds?: number | undefined;
}

/** DeploymentStatus is the most recently observed status of the Deployment. */
export interface DeploymentStatus {
    /**
     * The generation observed by the deployment controller.
     * +optional
     */
    observedGeneration?: number | undefined;
    /**
     * Total number of non-terminating pods targeted by this deployment (their labels match the selector).
     * +optional
     */
    replicas?: number | undefined;
    /**
     * Total number of non-terminating pods targeted by this deployment that have the desired template spec.
     * +optional
     */
    updatedReplicas?: number | undefined;
    /**
     * Total number of non-terminating pods targeted by this Deployment with a Ready Condition.
     * +optional
     */
    readyReplicas?: number | undefined;
    /**
     * Total number of available non-terminating pods (ready for at least minReadySeconds) targeted by this deployment.
     * +optional
     */
    availableReplicas?: number | undefined;
    /**
     * Total number of unavailable pods targeted by this deployment. This is the total number of
     * pods that are still required for the deployment to have 100% available capacity. They may
     * either be pods that are running but not yet available or pods that still have not been created.
     * +optional
     */
    unavailableReplicas?: number | undefined;
    /**
     * Total number of terminating pods targeted by this deployment. Terminating pods have a non-null
     * .metadata.deletionTimestamp and have not yet reached the Failed or Succeeded .status.phase.
     *
     * This is a beta field and requires enabling DeploymentReplicaSetTerminatingReplicas feature (enabled by default).
     * +optional
     */
    terminatingReplicas?: number | undefined;
    /**
     * Represents the latest available observations of a deployment's current state.
     * +patchMergeKey=type
     * +patchStrategy=merge
     * +listType=map
     * +listMapKey=type
     */
    conditions: DeploymentCondition[];
    /**
     * Count of hash collisions for the Deployment. The Deployment controller uses this
     * field as a collision avoidance mechanism when it needs to create the name for the
     * newest ReplicaSet.
     * +optional
     */
    collisionCount?: number | undefined;
}

/** DeploymentStrategy describes how to replace existing pods with new ones. */
export interface DeploymentStrategy {
    /**
     * type is the type of deployment. Can be "Recreate" or "RollingUpdate". Default is RollingUpdate.
     * +optional
     */
    type?: string | undefined;
    /**
     * rollingUpdate is the rolling update config params. Present only if DeploymentStrategyType =
     * RollingUpdate.
     * ---
     * TODO: Update this to follow our convention for oneOf, whatever we decide it
     * to be.
     * +optional
     */
    rollingUpdate?: RollingUpdateDeployment | undefined;
}

/**
 * HTTPIngressPath associates a path with a backend. Incoming urls matching the
 * path are forwarded to the backend.
 */
export interface HTTPIngressPath {
    /**
     * path is matched against the path of an incoming request. Currently it can
     * contain characters disallowed from the conventional "path" part of a URL
     * as defined by RFC 3986. Paths must begin with a '/'. When unspecified,
     * all paths from incoming requests are matched.
     * +optional
     */
    path?: string | undefined;
    /**
     * pathType determines the interpretation of the Path matching. PathType can
     * be one of the following values:
     * * Exact: Matches the URL path exactly.
     * * Prefix: Matches based on a URL path prefix split by '/'. Matching is
     *   done on a path element by element basis. A path element refers is the
     *   list of labels in the path split by the '/' separator. A request is a
     *   match for path p if every p is an element-wise prefix of p of the
     *   request path. Note that if the last element of the path is a substring
     *   of the last element in request path, it is not a match (e.g. /foo/bar
     *   matches /foo/bar/baz, but does not match /foo/barbaz).
     * * ImplementationSpecific: Interpretation of the Path matching is up to
     *   the IngressClass. Implementations can treat this as a separate PathType
     *   or treat it identically to Prefix or Exact path types.
     * Implementations are required to support all path types.
     * Defaults to ImplementationSpecific.
     */
    pathType?: string | undefined;
    /**
     * backend defines the referenced service endpoint to which the traffic
     * will be forwarded to.
     */
    backend?: IngressBackend | undefined;
}

/**
 * HTTPIngressRuleValue is a list of http selectors pointing to backends.
 * In the example: http://<host>/<path>?<searchpart> -> backend where
 * where parts of the url correspond to RFC 3986, this resource will be used
 * to match against everything after the last '/' and before the first '?'
 * or '#'.
 */
export interface HTTPIngressRuleValue {
    /**
     * paths is a collection of paths that map requests to backends.
     * +listType=atomic
     */
    paths: HTTPIngressPath[];
}

/**
 * DEPRECATED 1.9 - This group version of IPBlock is deprecated by networking/v1/IPBlock.
 * IPBlock describes a particular CIDR (Ex. "192.168.1.0/24","2001:db8::/64") that is allowed
 * to the pods matched by a NetworkPolicySpec's podSelector. The except entry describes CIDRs
 * that should not be included within this rule.
 */
export interface IPBlock {
    /**
     * cidr is a string representing the IP Block
     * Valid examples are "192.168.1.0/24" or "2001:db8::/64"
     * +required
     * +k8s:beta(since: "1.37")=+k8s:required
     */
    cidr?: string | undefined;
    /**
     * except is a slice of CIDRs that should not be included within an IP Block
     * Valid examples are "192.168.1.0/24" or "2001:db8::/64"
     * Except values will be rejected if they are outside the CIDR range
     * +optional
     * +listType=atomic
     */
    except: string[];
}

/**
 * Ingress is a collection of rules that allow inbound connections to reach the
 * endpoints defined by a backend. An Ingress can be configured to give services
 * externally-reachable urls, load balance traffic, terminate SSL, offer name
 * based virtual hosting etc.
 * DEPRECATED - This group version of Ingress is deprecated by networking.k8s.io/v1beta1 Ingress. See the release notes for more information.
 * +k8s:supportsSubresource="/status"
 */
export interface Ingress {
    /**
     * metadata is the standard object metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * spec is the desired state of the Ingress.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * +optional
     */
    spec?: IngressSpec | undefined;
    /**
     * status is the current state of the Ingress.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * +optional
     */
    status?: IngressStatus | undefined;
}

/** IngressBackend describes all endpoints for a given service and port. */
export interface IngressBackend {
    /**
     * serviceName specifies the name of the referenced service.
     * +optional
     */
    serviceName?: string | undefined;
    /**
     * servicePort specifies the port of the referenced service.
     * +optional
     */
    servicePort?: IntOrString | undefined;
    /**
     * resource is an ObjectRef to another Kubernetes resource in the namespace
     * of the Ingress object. If resource is specified, serviceName and servicePort
     * must not be specified.
     * +optional
     */
    resource?: TypedLocalObjectReference | undefined;
}

/** IngressList is a collection of Ingress. */
export interface IngressList {
    /**
     * Standard object's metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ListMeta | undefined;
    /** Items is the list of Ingress. */
    items: Ingress[];
}

/** IngressLoadBalancerIngress represents the status of a load-balancer ingress point. */
export interface IngressLoadBalancerIngress {
    /**
     * ip is set for load-balancer ingress points that are IP based.
     * +optional
     */
    ip?: string | undefined;
    /**
     * hostname is set for load-balancer ingress points that are DNS based.
     * +optional
     */
    hostname?: string | undefined;
    /**
     * ports provides information about the ports exposed by this LoadBalancer.
     * +listType=atomic
     * +optional
     */
    ports: IngressPortStatus[];
}

/** LoadBalancerStatus represents the status of a load-balancer. */
export interface IngressLoadBalancerStatus {
    /**
     * ingress is a list containing ingress points for the load-balancer.
     * +optional
     * +listType=atomic
     */
    ingress: IngressLoadBalancerIngress[];
}

/** IngressPortStatus represents the error condition of a service port */
export interface IngressPortStatus {
    /** port is the port number of the ingress port. */
    port?: number | undefined;
    /**
     * protocol is the protocol of the ingress port.
     * The supported values are: "TCP", "UDP", "SCTP"
     */
    protocol?: string | undefined;
    /**
     * error is to record the problem with the service port
     * The format of the error shall comply with the following rules:
     * - built-in error values shall be specified in this file and those shall use
     *   CamelCase names
     * - cloud provider specific error values must have names that comply with the
     *   format foo.example.com/CamelCase.
     * ---
     * The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
     * +optional
     * +kubebuilder:validation:Required
     * +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)* /)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
     * +kubebuilder:validation:MaxLength=316
     */
    error?: string | undefined;
}

/**
 * IngressRule represents the rules mapping the paths under a specified host to
 * the related backend services. Incoming requests are first evaluated for a host
 * match, then routed to the backend associated with the matching IngressRuleValue.
 */
export interface IngressRule {
    /**
     * host is the fully qualified domain name of a network host, as defined by RFC 3986.
     * Note the following deviations from the "host" part of the
     * URI as defined in RFC 3986:
     * 1. IPs are not allowed. Currently an IngressRuleValue can only apply to
     *    the IP in the Spec of the parent Ingress.
     * 2. The `:` delimiter is not respected because ports are not allowed.
     * 	  Currently the port of an Ingress is implicitly :80 for http and
     * 	  :443 for https.
     * Both these may change in the future.
     * Incoming requests are matched against the host before the
     * IngressRuleValue. If the host is unspecified, the Ingress routes all
     * traffic based on the specified IngressRuleValue.
     *
     * Host can be "precise" which is a domain name without the terminating dot of
     * a network host (e.g. "foo.bar.com") or "wildcard", which is a domain name
     * prefixed with a single wildcard label (e.g. "*.foo.com").
     * The wildcard character '*' must appear by itself as the first DNS label and
     * matches only a single label. You cannot have a wildcard label by itself (e.g. Host == "*").
     * Requests will be matched against the Host field in the following way:
     * 1. If Host is precise, the request matches this rule if the http host header is equal to Host.
     * 2. If Host is a wildcard, then the request matches this rule if the http host header
     * is to equal to the suffix (removing the first label) of the wildcard rule.
     * +optional
     */
    host?: string | undefined;
    /**
     * IngressRuleValue represents a rule to route requests for this IngressRule.
     * If unspecified, the rule defaults to a http catch-all. Whether that sends
     * just traffic matching the host to the default backend or all traffic to the
     * default backend, is left to the controller fulfilling the Ingress. Http is
     * currently the only supported IngressRuleValue.
     * +optional
     */
    ingressRuleValue?: IngressRuleValue | undefined;
}

/**
 * IngressRuleValue represents a rule to apply against incoming requests. If the
 * rule is satisfied, the request is routed to the specified backend. Currently
 * mixing different types of rules in a single Ingress is disallowed, so exactly
 * one of the following must be set.
 */
export interface IngressRuleValue {
    /**
     * http is a list of http selectors pointing to backends.
     * A path is matched against the path of an incoming request. Currently it can
     * contain characters disallowed from the conventional "path" part of a URL
     * as defined by RFC 3986. Paths must begin with a '/'.
     * A backend defines the referenced service endpoint to which the traffic
     * will be forwarded to.
     */
    http?: HTTPIngressRuleValue | undefined;
}

/** IngressSpec describes the Ingress the user wishes to exist. */
export interface IngressSpec {
    /**
     * ingressClassName is the name of the IngressClass cluster resource. The
     * associated IngressClass defines which controller will implement the
     * resource. This replaces the deprecated `kubernetes.io/ingress.class`
     * annotation. For backwards compatibility, when that annotation is set, it
     * must be given precedence over this field. The controller may emit a
     * warning if the field and annotation have different values.
     * Implementations of this API should ignore Ingresses without a class
     * specified. An IngressClass resource may be marked as default, which can
     * be used to set a default value for this field. For more information,
     * refer to the IngressClass documentation.
     * +optional
     */
    ingressClassName?: string | undefined;
    /**
     * backend is a default backend capable of servicing requests that don't match any
     * rule. At least one of 'backend' or 'rules' must be specified. This field
     * is optional to allow the loadbalancer controller or defaulting logic to
     * specify a global default.
     * +optional
     */
    backend?: IngressBackend | undefined;
    /**
     * tls is TLS configuration. Currently the Ingress only supports a single TLS
     * port, 443. If multiple members of this list specify different hosts, they
     * will be multiplexed on the same port according to the hostname specified
     * through the SNI TLS extension, if the ingress controller fulfilling the
     * ingress supports SNI.
     * +optional
     * +listType=atomic
     */
    tls: IngressTLS[];
    /**
     * rules is a list of host rules used to configure the Ingress. If unspecified, or
     * no rule matches, all traffic is sent to the default backend.
     * +optional
     * +listType=atomic
     */
    rules: IngressRule[];
}

/** ingressStatus describe the current state of the Ingress. */
export interface IngressStatus {
    /**
     * loadBalancer contains the current status of the load-balancer.
     * +optional
     */
    loadBalancer?: IngressLoadBalancerStatus | undefined;
}

/** IngressTLS describes the transport layer security associated with an Ingress. */
export interface IngressTLS {
    /**
     * hosts are a list of hosts included in the TLS certificate. The values in
     * this list must match the name/s used in the tlsSecret. Defaults to the
     * wildcard host setting for the loadbalancer controller fulfilling this
     * Ingress, if left unspecified.
     * +optional
     * +listType=atomic
     */
    hosts: string[];
    /**
     * secretName is the name of the secret used to terminate SSL traffic on 443.
     * Field is left optional to allow SSL routing based on SNI hostname alone.
     * If the SNI host in a listener conflicts with the "Host" header field used
     * by an IngressRule, the SNI host is used for termination and value of the
     * Host header is used for routing.
     * +optional
     */
    secretName?: string | undefined;
}

/**
 * DEPRECATED 1.9 - This group version of NetworkPolicy is deprecated by networking/v1/NetworkPolicy.
 * NetworkPolicy describes what network traffic is allowed for a set of Pods
 */
export interface NetworkPolicy {
    /**
     * metadata is the standard object metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * spec is the specification of the desired behavior for this NetworkPolicy.
     * +optional
     */
    spec?: NetworkPolicySpec | undefined;
}

/**
 * DEPRECATED 1.9 - This group version of NetworkPolicyEgressRule is deprecated by networking/v1/NetworkPolicyEgressRule.
 * NetworkPolicyEgressRule describes a particular set of traffic that is allowed out of pods
 * matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and to.
 * This type is beta-level in 1.8
 */
export interface NetworkPolicyEgressRule {
    /**
     * ports is the list of destination ports for outgoing traffic.
     * Each item in this list is combined using a logical OR. If this field is
     * empty or missing, this rule matches all ports (traffic not restricted by port).
     * If this field is present and contains at least one item, then this rule allows
     * traffic only if the traffic matches at least one port in the list.
     * +optional
     * +listType=atomic
     */
    ports: NetworkPolicyPort[];
    /**
     * to is the list of destinations for outgoing traffic of pods selected for this rule.
     * Items in this list are combined using a logical OR operation. If this field is
     * empty or missing, this rule matches all destinations (traffic not restricted by
     * destination). If this field is present and contains at least one item, this rule
     * allows traffic only if the traffic matches at least one item in the to list.
     * +optional
     * +listType=atomic
     * +k8s:beta(since: "1.37")=+k8s:optional
     */
    to: NetworkPolicyPeer[];
}

/**
 * DEPRECATED 1.9 - This group version of NetworkPolicyIngressRule is deprecated by networking/v1/NetworkPolicyIngressRule.
 * This NetworkPolicyIngressRule matches traffic if and only if the traffic matches both ports AND from.
 */
export interface NetworkPolicyIngressRule {
    /**
     * ports is the list of ports which should be made accessible on the pods selected for this rule.
     * Each item in this list is combined using a logical OR.
     * If this field is empty or missing, this rule matches all ports (traffic not restricted by port).
     * If this field is present and contains at least one item, then this rule allows traffic
     * only if the traffic matches at least one port in the list.
     * +optional
     * +listType=atomic
     */
    ports: NetworkPolicyPort[];
    /**
     * from is the list of sources which should be able to access the pods selected for this rule.
     * Items in this list are combined using a logical OR operation.
     * If this field is empty or missing, this rule matches all sources (traffic not restricted by source).
     * If this field is present and contains at least one item, this rule allows traffic only if the
     * traffic matches at least one item in the from list.
     * +optional
     * +listType=atomic
     * +k8s:beta(since: "1.37")=+k8s:optional
     */
    from: NetworkPolicyPeer[];
}

/**
 * DEPRECATED 1.9 - This group version of NetworkPolicyList is deprecated by networking/v1/NetworkPolicyList.
 * Network Policy List is a list of NetworkPolicy objects.
 */
export interface NetworkPolicyList {
    /**
     * Standard list metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ListMeta | undefined;
    /** Items is a list of schema objects. */
    items: NetworkPolicy[];
}

/** DEPRECATED 1.9 - This group version of NetworkPolicyPeer is deprecated by networking/v1/NetworkPolicyPeer. */
export interface NetworkPolicyPeer {
    /**
     * podSelector is a label selector which selects Pods. This field follows standard label
     * selector semantics; if present but empty, it selects all pods.
     *
     * If NamespaceSelector is also set, then the NetworkPolicyPeer as a whole selects
     * the Pods matching PodSelector in the Namespaces selected by NamespaceSelector.
     * Otherwise it selects the Pods matching PodSelector in the policy's own Namespace.
     * +optional
     */
    podSelector?: LabelSelector | undefined;
    /**
     * namespaceSelector selects Namespaces using cluster-scoped labels. This field follows standard label
     * selector semantics; if present but empty, it selects all namespaces.
     *
     * If PodSelector is also set, then the NetworkPolicyPeer as a whole selects
     * the Pods matching PodSelector in the Namespaces selected by NamespaceSelector.
     * Otherwise it selects all Pods in the Namespaces selected by NamespaceSelector.
     * +optional
     */
    namespaceSelector?: LabelSelector | undefined;
    /**
     * ipBlock defines policy on a particular IPBlock. If this field is set then
     * neither of the other fields can be.
     * +optional
     * +k8s:beta(since: "1.37")=+k8s:optional
     */
    ipBlock?: IPBlock | undefined;
}

/** DEPRECATED 1.9 - This group version of NetworkPolicyPort is deprecated by networking/v1/NetworkPolicyPort. */
export interface NetworkPolicyPort {
    /**
     * protocol is the protocol (TCP, UDP, or SCTP) which traffic must match.
     * If not specified, this field defaults to TCP.
     * +optional
     */
    protocol?: string | undefined;
    /**
     * port is the port on the given protocol. This can either be a numerical or named
     * port on a pod. If this field is not provided, this matches all port names and
     * numbers.
     * If present, only traffic on the specified protocol AND port will be matched.
     * +optional
     */
    port?: IntOrString | undefined;
    /**
     * endPort indicates that the range of ports from port to endPort, inclusive,
     * should be allowed by the policy. This field cannot be defined if the port field
     * is not defined or if the port field is defined as a named (string) port.
     * The endPort must be equal or greater than port.
     * +optional
     */
    endPort?: number | undefined;
}

/** DEPRECATED 1.9 - This group version of NetworkPolicySpec is deprecated by networking/v1/NetworkPolicySpec. */
export interface NetworkPolicySpec {
    /**
     * podSelector selects the pods to which this NetworkPolicy object applies.  The array of ingress rules
     * is applied to any pods selected by this field. Multiple network policies can select the
     * same set of pods.  In this case, the ingress rules for each are combined additively.
     * This field is NOT optional and follows standard label selector semantics.
     * An empty podSelector matches all pods in this namespace.
     */
    podSelector?: LabelSelector | undefined;
    /**
     * ingress is the list of ingress rules to be applied to the selected pods.
     * Traffic is allowed to a pod if there are no NetworkPolicies selecting the pod
     * OR if the traffic source is the pod's local node,
     * OR if the traffic matches at least one ingress rule across all of the NetworkPolicy
     * objects whose podSelector matches the pod.
     * If this field is empty then this NetworkPolicy does not allow any traffic
     * (and serves solely to ensure that the pods it selects are isolated by default).
     * +optional
     * +listType=atomic
     * +k8s:beta(since: "1.37")=+k8s:optional
     */
    ingress: NetworkPolicyIngressRule[];
    /**
     * egress is the list of egress rules to be applied to the selected pods. Outgoing traffic is
     * allowed if there are no NetworkPolicies selecting the pod (and cluster policy
     * otherwise allows the traffic), OR if the traffic matches at least one egress rule
     * across all of the NetworkPolicy objects whose podSelector matches the pod. If
     * this field is empty then this NetworkPolicy limits all outgoing traffic (and serves
     * solely to ensure that the pods it selects are isolated by default).
     * This field is beta-level in 1.8
     * +optional
     * +listType=atomic
     * +k8s:beta(since: "1.37")=+k8s:optional
     */
    egress: NetworkPolicyEgressRule[];
    /**
     * policyTypes is the list of rule types that the NetworkPolicy relates to.
     * Valid options are ["Ingress"], ["Egress"], or ["Ingress", "Egress"].
     * If this field is not specified, it will default based on the existence of Ingress or Egress rules;
     * policies that contain an Egress section are assumed to affect Egress, and all policies
     * (whether or not they contain an Ingress section) are assumed to affect Ingress.
     * If you want to write an egress-only policy, you must explicitly specify policyTypes [ "Egress" ].
     * Likewise, if you want to write a policy that specifies that no egress is allowed,
     * you must specify a policyTypes value that include "Egress" (since such a policy would not include
     * an Egress section and would otherwise default to just [ "Ingress" ]).
     * This field is beta-level in 1.8
     * +optional
     * +listType=atomic
     */
    policyTypes: string[];
}

/**
 * DEPRECATED - This group version of ReplicaSet is deprecated by apps/v1beta2/ReplicaSet. See the release notes for
 * more information.
 * ReplicaSet ensures that a specified number of pod replicas are running at any given time.
 * +k8s:supportsSubresource="/status"
 */
export interface ReplicaSet {
    /**
     * metadata is the standard object metadata.
     * If the Labels of a ReplicaSet are empty, they are defaulted to
     * be the same as the Pod(s) that the ReplicaSet manages.
     * Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * spec defines the specification of the desired behavior of the ReplicaSet.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * +optional
     */
    spec?: ReplicaSetSpec | undefined;
    /**
     * status is the most recently observed status of the ReplicaSet.
     * This data may be out of date by some window of time.
     * Populated by the system.
     * Read-only.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * +optional
     */
    status?: ReplicaSetStatus | undefined;
}

/** ReplicaSetCondition describes the state of a replica set at a certain point. */
export interface ReplicaSetCondition {
    /** type of replica set condition. */
    type?: string | undefined;
    /** status of the condition, one of True, False, Unknown. */
    status?: string | undefined;
    /**
     * lastTransitionTime is the last time the condition transitioned from one status to another.
     * +optional
     */
    lastTransitionTime?: Time | undefined;
    /**
     * reason is the reason for the condition's last transition.
     * +optional
     */
    reason?: string | undefined;
    /**
     * message is a human readable message indicating details about the transition.
     * +optional
     */
    message?: string | undefined;
}

/** ReplicaSetList is a collection of ReplicaSets. */
export interface ReplicaSetList {
    /**
     * Standard list metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
     * +optional
     */
    metadata?: ListMeta | undefined;
    /**
     * List of ReplicaSets.
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset
     */
    items: ReplicaSet[];
}

/** ReplicaSetSpec is the specification of a ReplicaSet. */
export interface ReplicaSetSpec {
    /**
     * replicas is the number of desired pods.
     * This is a pointer to distinguish between explicit zero and unspecified.
     * Defaults to 1.
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset
     * +optional
     */
    replicas?: number | undefined;
    /**
     * minReadySeconds is the minimum number of seconds for which a newly created pod should be ready
     * without any of its container crashing, for it to be considered available.
     * Defaults to 0 (pod will be considered available as soon as it is ready)
     * +optional
     */
    minReadySeconds?: number | undefined;
    /**
     * selector is a label query over pods that should match the replica count.
     * If the selector is empty, it is defaulted to the labels present on the pod template.
     * Label keys and values that must match in order to be controlled by this replica set.
     * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
     * +optional
     */
    selector?: LabelSelector | undefined;
    /**
     * template is the object that describes the pod that will be created if
     * insufficient replicas are detected.
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/#pod-template
     * +optional
     */
    template?: PodTemplateSpec | undefined;
}

/** ReplicaSetStatus represents the current status of a ReplicaSet. */
export interface ReplicaSetStatus {
    /**
     * Replicas is the most recently observed number of non-terminating pods.
     * More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset
     */
    replicas?: number | undefined;
    /**
     * The number of non-terminating pods that have labels matching the labels of the pod template of the replicaset.
     * +optional
     */
    fullyLabeledReplicas?: number | undefined;
    /**
     * The number of non-terminating pods targeted by this ReplicaSet with a Ready Condition.
     * +optional
     */
    readyReplicas?: number | undefined;
    /**
     * The number of available non-terminating pods (ready for at least minReadySeconds) for this replica set.
     * +optional
     */
    availableReplicas?: number | undefined;
    /**
     * The number of terminating pods for this replica set. Terminating pods have a non-null .metadata.deletionTimestamp
     * and have not yet reached the Failed or Succeeded .status.phase.
     *
     * This is a beta field and requires enabling DeploymentReplicaSetTerminatingReplicas feature (enabled by default).
     * +optional
     */
    terminatingReplicas?: number | undefined;
    /**
     * ObservedGeneration reflects the generation of the most recently observed ReplicaSet.
     * +optional
     */
    observedGeneration?: number | undefined;
    /**
     * Represents the latest available observations of a replica set's current state.
     * +optional
     * +patchMergeKey=type
     * +patchStrategy=merge
     * +listType=map
     * +listMapKey=type
     */
    conditions: ReplicaSetCondition[];
}

/** DEPRECATED. */
export interface RollbackConfig {
    /**
     * revision is the revision to rollback to. If set to 0, rollback to the last revision.
     * +optional
     */
    revision?: number | undefined;
}

/** Spec to control the desired behavior of daemon set rolling update. */
export interface RollingUpdateDaemonSet {
    /**
     * maxUnavailable is the maximum number of DaemonSet pods that can be unavailable during the
     * update. Value can be an absolute number (ex: 5) or a percentage of total
     * number of DaemonSet pods at the start of the update (ex: 10%). Absolute
     * number is calculated from percentage by rounding up.
     * This cannot be 0 if MaxSurge is 0
     * Default value is 1.
     * Example: when this is set to 30%, at most 30% of the total number of nodes
     * that should be running the daemon pod (i.e. status.desiredNumberScheduled)
     * can have their pods stopped for an update at any given time. The update
     * starts by stopping at most 30% of those DaemonSet pods and then brings
     * up new DaemonSet pods in their place. Once the new pods are available,
     * it then proceeds onto other DaemonSet pods, thus ensuring that at least
     * 70% of original number of DaemonSet pods are available at all times during
     * the update.
     * +optional
     */
    maxUnavailable?: IntOrString | undefined;
    /**
     * maxSurge is the maximum number of nodes with an existing available DaemonSet pod that
     * can have an updated DaemonSet pod during during an update.
     * Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).
     * This can not be 0 if MaxUnavailable is 0.
     * Absolute number is calculated from percentage by rounding up to a minimum of 1.
     * Default value is 0.
     * Example: when this is set to 30%, at most 30% of the total number of nodes
     * that should be running the daemon pod (i.e. status.desiredNumberScheduled)
     * can have their a new pod created before the old pod is marked as deleted.
     * The update starts by launching new pods on 30% of nodes. Once an updated
     * pod is available (Ready for at least minReadySeconds) the old DaemonSet pod
     * on that node is marked deleted. If the old pod becomes unavailable for any
     * reason (Ready transitions to false, is evicted, or is drained) an updated
     * pod is immediately created on that node without considering surge limits.
     * Allowing surge implies the possibility that the resources consumed by the
     * daemonset on any given node can double if the readiness check fails, and
     * so resource intensive daemonsets should take into account that they may
     * cause evictions during disruption.
     * This is an alpha field and requires enabling DaemonSetUpdateSurge feature gate.
     * +optional
     */
    maxSurge?: IntOrString | undefined;
}

/** Spec to control the desired behavior of rolling update. */
export interface RollingUpdateDeployment {
    /**
     * maxUnavailable is the maximum number of pods that can be unavailable during the update.
     * Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).
     * Absolute number is calculated from percentage by rounding down.
     * This can not be 0 if MaxSurge is 0.
     * By default, a fixed value of 1 is used.
     * Example: when this is set to 30%, the old RC can be scaled down to 70% of desired pods
     * immediately when the rolling update starts. Once new pods are ready, old RC
     * can be scaled down further, followed by scaling up the new RC, ensuring
     * that the total number of pods available at all times during the update is at
     * least 70% of desired pods.
     * +optional
     */
    maxUnavailable?: IntOrString | undefined;
    /**
     * maxSurge is the maximum number of pods that can be scheduled above the desired number of
     * pods.
     * Value can be an absolute number (ex: 5) or a percentage of desired pods (ex: 10%).
     * This can not be 0 if MaxUnavailable is 0.
     * Absolute number is calculated from percentage by rounding up.
     * By default, a value of 1 is used.
     * Example: when this is set to 30%, the new RC can be scaled up immediately when
     * the rolling update starts, such that the total number of old and new pods do not exceed
     * 130% of desired pods. Once old pods have been killed,
     * new RC can be scaled up further, ensuring that total number of pods running
     * at any time during the update is at most 130% of desired pods.
     * +optional
     */
    maxSurge?: IntOrString | undefined;
}

/** represents a scaling request for a resource. */
export interface Scale {
    /**
     * metadata is the standard object metadata; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata.
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * spec defines the behavior of the scale. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status.
     * +optional
     */
    spec?: ScaleSpec | undefined;
    /**
     * status is current status of the scale. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status. Read-only.
     * +optional
     */
    status?: ScaleStatus | undefined;
}

/** describes the attributes of a scale subresource */
export interface ScaleSpec {
    /**
     * replicas is the desired number of instances for the scaled object.
     * +optional
     * +k8s:beta(since: "1.37")=+k8s:optional
     * +default=0
     * +k8s:beta(since: "1.37")=+k8s:minimum=0
     */
    replicas?: number | undefined;
}

/** represents the current status of a scale subresource. */
export interface ScaleStatus {
    /** replicas is the actual number of observed instances of the scaled object. */
    replicas?: number | undefined;
    /**
     * selector is a label query over pods that should match the replicas count. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
     * +optional
     * +mapType=atomic
     */
    selector: { [key: string]: string };
    /**
     * targetSelector is the label selector for pods that should match the replicas count. This is a serializated
     * version of both map-based and more expressive set-based selectors. This is done to
     * avoid introspection in the clients. The string will be in the same format as the
     * query-param syntax. If the target type only supports map-based selectors, both this
     * field and map-based selector field are populated.
     * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
     * +optional
     */
    targetSelector?: string | undefined;
}

export interface ScaleStatus_SelectorEntry {
    key: string;
    value: string;
}

function createBaseDaemonSet(): DaemonSet {
    return { metadata: undefined, spec: undefined, status: undefined };
}

export const DaemonSet: MessageFns<DaemonSet> = {
    encode(message: DaemonSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.spec !== undefined) {
            DaemonSetSpec.encode(message.spec, writer.uint32(18).fork()).join();
        }
        if (message.status !== undefined) {
            DaemonSetStatus.encode(message.status, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DaemonSet {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDaemonSet();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.spec = DaemonSetSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.status = DaemonSetStatus.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DaemonSet {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            spec: isSet(object.spec) ? DaemonSetSpec.fromJSON(object.spec) : undefined,
            status: isSet(object.status) ? DaemonSetStatus.fromJSON(object.status) : undefined,
        };
    },

    toJSON(message: DaemonSet): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.spec !== undefined) {
            obj.spec = DaemonSetSpec.toJSON(message.spec);
        }
        if (message.status !== undefined) {
            obj.status = DaemonSetStatus.toJSON(message.status);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DaemonSet>, I>>(base?: I): DaemonSet {
        return DaemonSet.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DaemonSet>, I>>(object: I): DaemonSet {
        const message = createBaseDaemonSet();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.spec =
            object.spec !== undefined && object.spec !== null
                ? DaemonSetSpec.fromPartial(object.spec)
                : undefined;
        message.status =
            object.status !== undefined && object.status !== null
                ? DaemonSetStatus.fromPartial(object.status)
                : undefined;
        return message;
    },
};

function createBaseDaemonSetCondition(): DaemonSetCondition {
    return { type: '', status: '', lastTransitionTime: undefined, reason: '', message: '' };
}

export const DaemonSetCondition: MessageFns<DaemonSetCondition> = {
    encode(message: DaemonSetCondition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.type !== undefined && message.type !== '') {
            writer.uint32(10).string(message.type);
        }
        if (message.status !== undefined && message.status !== '') {
            writer.uint32(18).string(message.status);
        }
        if (message.lastTransitionTime !== undefined) {
            Time.encode(message.lastTransitionTime, writer.uint32(26).fork()).join();
        }
        if (message.reason !== undefined && message.reason !== '') {
            writer.uint32(34).string(message.reason);
        }
        if (message.message !== undefined && message.message !== '') {
            writer.uint32(42).string(message.message);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DaemonSetCondition {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDaemonSetCondition();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.type = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.status = reader.string();
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.lastTransitionTime = Time.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.reason = reader.string();
                        continue;
                    }
                    case 5: {
                        if (tag !== 42) {
                            break;
                        }

                        message.message = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DaemonSetCondition {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : '',
            status: isSet(object.status) ? globalThis.String(object.status) : '',
            lastTransitionTime: isSet(object.lastTransitionTime)
                ? Time.fromJSON(object.lastTransitionTime)
                : undefined,
            reason: isSet(object.reason) ? globalThis.String(object.reason) : '',
            message: isSet(object.message) ? globalThis.String(object.message) : '',
        };
    },

    toJSON(message: DaemonSetCondition): unknown {
        const obj: any = {};
        if (message.type !== undefined && message.type !== '') {
            obj.type = message.type;
        }
        if (message.status !== undefined && message.status !== '') {
            obj.status = message.status;
        }
        if (message.lastTransitionTime !== undefined) {
            obj.lastTransitionTime = Time.toJSON(message.lastTransitionTime);
        }
        if (message.reason !== undefined && message.reason !== '') {
            obj.reason = message.reason;
        }
        if (message.message !== undefined && message.message !== '') {
            obj.message = message.message;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DaemonSetCondition>, I>>(base?: I): DaemonSetCondition {
        return DaemonSetCondition.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DaemonSetCondition>, I>>(object: I): DaemonSetCondition {
        const message = createBaseDaemonSetCondition();
        message.type = object.type ?? '';
        message.status = object.status ?? '';
        message.lastTransitionTime =
            object.lastTransitionTime !== undefined && object.lastTransitionTime !== null
                ? Time.fromPartial(object.lastTransitionTime)
                : undefined;
        message.reason = object.reason ?? '';
        message.message = object.message ?? '';
        return message;
    },
};

function createBaseDaemonSetList(): DaemonSetList {
    return { metadata: undefined, items: [] };
}

export const DaemonSetList: MessageFns<DaemonSetList> = {
    encode(message: DaemonSetList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            DaemonSet.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DaemonSetList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDaemonSetList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(DaemonSet.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DaemonSetList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => DaemonSet.fromJSON(e))
                : [],
        };
    },

    toJSON(message: DaemonSetList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => DaemonSet.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DaemonSetList>, I>>(base?: I): DaemonSetList {
        return DaemonSetList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DaemonSetList>, I>>(object: I): DaemonSetList {
        const message = createBaseDaemonSetList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => DaemonSet.fromPartial(e)) || [];
        return message;
    },
};

function createBaseDaemonSetSpec(): DaemonSetSpec {
    return {
        selector: undefined,
        template: undefined,
        updateStrategy: undefined,
        minReadySeconds: 0,
        templateGeneration: 0,
        revisionHistoryLimit: 0,
    };
}

export const DaemonSetSpec: MessageFns<DaemonSetSpec> = {
    encode(message: DaemonSetSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.selector !== undefined) {
            LabelSelector.encode(message.selector, writer.uint32(10).fork()).join();
        }
        if (message.template !== undefined) {
            PodTemplateSpec.encode(message.template, writer.uint32(18).fork()).join();
        }
        if (message.updateStrategy !== undefined) {
            DaemonSetUpdateStrategy.encode(message.updateStrategy, writer.uint32(26).fork()).join();
        }
        if (message.minReadySeconds !== undefined && message.minReadySeconds !== 0) {
            writer.uint32(32).int32(message.minReadySeconds);
        }
        if (message.templateGeneration !== undefined && message.templateGeneration !== 0) {
            writer.uint32(40).int64(message.templateGeneration);
        }
        if (message.revisionHistoryLimit !== undefined && message.revisionHistoryLimit !== 0) {
            writer.uint32(48).int32(message.revisionHistoryLimit);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DaemonSetSpec {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDaemonSetSpec();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.selector = LabelSelector.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.template = PodTemplateSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.updateStrategy = DaemonSetUpdateStrategy.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 32) {
                            break;
                        }

                        message.minReadySeconds = reader.int32();
                        continue;
                    }
                    case 5: {
                        if (tag !== 40) {
                            break;
                        }

                        message.templateGeneration = longToNumber(reader.int64());
                        continue;
                    }
                    case 6: {
                        if (tag !== 48) {
                            break;
                        }

                        message.revisionHistoryLimit = reader.int32();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DaemonSetSpec {
        return {
            selector: isSet(object.selector) ? LabelSelector.fromJSON(object.selector) : undefined,
            template: isSet(object.template) ? PodTemplateSpec.fromJSON(object.template) : undefined,
            updateStrategy: isSet(object.updateStrategy)
                ? DaemonSetUpdateStrategy.fromJSON(object.updateStrategy)
                : undefined,
            minReadySeconds: isSet(object.minReadySeconds) ? globalThis.Number(object.minReadySeconds) : 0,
            templateGeneration: isSet(object.templateGeneration)
                ? globalThis.Number(object.templateGeneration)
                : 0,
            revisionHistoryLimit: isSet(object.revisionHistoryLimit)
                ? globalThis.Number(object.revisionHistoryLimit)
                : 0,
        };
    },

    toJSON(message: DaemonSetSpec): unknown {
        const obj: any = {};
        if (message.selector !== undefined) {
            obj.selector = LabelSelector.toJSON(message.selector);
        }
        if (message.template !== undefined) {
            obj.template = PodTemplateSpec.toJSON(message.template);
        }
        if (message.updateStrategy !== undefined) {
            obj.updateStrategy = DaemonSetUpdateStrategy.toJSON(message.updateStrategy);
        }
        if (message.minReadySeconds !== undefined && message.minReadySeconds !== 0) {
            obj.minReadySeconds = Math.round(message.minReadySeconds);
        }
        if (message.templateGeneration !== undefined && message.templateGeneration !== 0) {
            obj.templateGeneration = Math.round(message.templateGeneration);
        }
        if (message.revisionHistoryLimit !== undefined && message.revisionHistoryLimit !== 0) {
            obj.revisionHistoryLimit = Math.round(message.revisionHistoryLimit);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DaemonSetSpec>, I>>(base?: I): DaemonSetSpec {
        return DaemonSetSpec.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DaemonSetSpec>, I>>(object: I): DaemonSetSpec {
        const message = createBaseDaemonSetSpec();
        message.selector =
            object.selector !== undefined && object.selector !== null
                ? LabelSelector.fromPartial(object.selector)
                : undefined;
        message.template =
            object.template !== undefined && object.template !== null
                ? PodTemplateSpec.fromPartial(object.template)
                : undefined;
        message.updateStrategy =
            object.updateStrategy !== undefined && object.updateStrategy !== null
                ? DaemonSetUpdateStrategy.fromPartial(object.updateStrategy)
                : undefined;
        message.minReadySeconds = object.minReadySeconds ?? 0;
        message.templateGeneration = object.templateGeneration ?? 0;
        message.revisionHistoryLimit = object.revisionHistoryLimit ?? 0;
        return message;
    },
};

function createBaseDaemonSetStatus(): DaemonSetStatus {
    return {
        currentNumberScheduled: 0,
        numberMisscheduled: 0,
        desiredNumberScheduled: 0,
        numberReady: 0,
        observedGeneration: 0,
        updatedNumberScheduled: 0,
        numberAvailable: 0,
        numberUnavailable: 0,
        collisionCount: 0,
        conditions: [],
    };
}

export const DaemonSetStatus: MessageFns<DaemonSetStatus> = {
    encode(message: DaemonSetStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.currentNumberScheduled !== undefined && message.currentNumberScheduled !== 0) {
            writer.uint32(8).int32(message.currentNumberScheduled);
        }
        if (message.numberMisscheduled !== undefined && message.numberMisscheduled !== 0) {
            writer.uint32(16).int32(message.numberMisscheduled);
        }
        if (message.desiredNumberScheduled !== undefined && message.desiredNumberScheduled !== 0) {
            writer.uint32(24).int32(message.desiredNumberScheduled);
        }
        if (message.numberReady !== undefined && message.numberReady !== 0) {
            writer.uint32(32).int32(message.numberReady);
        }
        if (message.observedGeneration !== undefined && message.observedGeneration !== 0) {
            writer.uint32(40).int64(message.observedGeneration);
        }
        if (message.updatedNumberScheduled !== undefined && message.updatedNumberScheduled !== 0) {
            writer.uint32(48).int32(message.updatedNumberScheduled);
        }
        if (message.numberAvailable !== undefined && message.numberAvailable !== 0) {
            writer.uint32(56).int32(message.numberAvailable);
        }
        if (message.numberUnavailable !== undefined && message.numberUnavailable !== 0) {
            writer.uint32(64).int32(message.numberUnavailable);
        }
        if (message.collisionCount !== undefined && message.collisionCount !== 0) {
            writer.uint32(72).int32(message.collisionCount);
        }
        for (const v of message.conditions) {
            DaemonSetCondition.encode(v!, writer.uint32(82).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DaemonSetStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDaemonSetStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.currentNumberScheduled = reader.int32();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }

                        message.numberMisscheduled = reader.int32();
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }

                        message.desiredNumberScheduled = reader.int32();
                        continue;
                    }
                    case 4: {
                        if (tag !== 32) {
                            break;
                        }

                        message.numberReady = reader.int32();
                        continue;
                    }
                    case 5: {
                        if (tag !== 40) {
                            break;
                        }

                        message.observedGeneration = longToNumber(reader.int64());
                        continue;
                    }
                    case 6: {
                        if (tag !== 48) {
                            break;
                        }

                        message.updatedNumberScheduled = reader.int32();
                        continue;
                    }
                    case 7: {
                        if (tag !== 56) {
                            break;
                        }

                        message.numberAvailable = reader.int32();
                        continue;
                    }
                    case 8: {
                        if (tag !== 64) {
                            break;
                        }

                        message.numberUnavailable = reader.int32();
                        continue;
                    }
                    case 9: {
                        if (tag !== 72) {
                            break;
                        }

                        message.collisionCount = reader.int32();
                        continue;
                    }
                    case 10: {
                        if (tag !== 82) {
                            break;
                        }

                        message.conditions.push(DaemonSetCondition.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DaemonSetStatus {
        return {
            currentNumberScheduled: isSet(object.currentNumberScheduled)
                ? globalThis.Number(object.currentNumberScheduled)
                : 0,
            numberMisscheduled: isSet(object.numberMisscheduled)
                ? globalThis.Number(object.numberMisscheduled)
                : 0,
            desiredNumberScheduled: isSet(object.desiredNumberScheduled)
                ? globalThis.Number(object.desiredNumberScheduled)
                : 0,
            numberReady: isSet(object.numberReady) ? globalThis.Number(object.numberReady) : 0,
            observedGeneration: isSet(object.observedGeneration)
                ? globalThis.Number(object.observedGeneration)
                : 0,
            updatedNumberScheduled: isSet(object.updatedNumberScheduled)
                ? globalThis.Number(object.updatedNumberScheduled)
                : 0,
            numberAvailable: isSet(object.numberAvailable) ? globalThis.Number(object.numberAvailable) : 0,
            numberUnavailable: isSet(object.numberUnavailable)
                ? globalThis.Number(object.numberUnavailable)
                : 0,
            collisionCount: isSet(object.collisionCount) ? globalThis.Number(object.collisionCount) : 0,
            conditions: globalThis.Array.isArray(object?.conditions)
                ? object.conditions.map((e: any) => DaemonSetCondition.fromJSON(e))
                : [],
        };
    },

    toJSON(message: DaemonSetStatus): unknown {
        const obj: any = {};
        if (message.currentNumberScheduled !== undefined && message.currentNumberScheduled !== 0) {
            obj.currentNumberScheduled = Math.round(message.currentNumberScheduled);
        }
        if (message.numberMisscheduled !== undefined && message.numberMisscheduled !== 0) {
            obj.numberMisscheduled = Math.round(message.numberMisscheduled);
        }
        if (message.desiredNumberScheduled !== undefined && message.desiredNumberScheduled !== 0) {
            obj.desiredNumberScheduled = Math.round(message.desiredNumberScheduled);
        }
        if (message.numberReady !== undefined && message.numberReady !== 0) {
            obj.numberReady = Math.round(message.numberReady);
        }
        if (message.observedGeneration !== undefined && message.observedGeneration !== 0) {
            obj.observedGeneration = Math.round(message.observedGeneration);
        }
        if (message.updatedNumberScheduled !== undefined && message.updatedNumberScheduled !== 0) {
            obj.updatedNumberScheduled = Math.round(message.updatedNumberScheduled);
        }
        if (message.numberAvailable !== undefined && message.numberAvailable !== 0) {
            obj.numberAvailable = Math.round(message.numberAvailable);
        }
        if (message.numberUnavailable !== undefined && message.numberUnavailable !== 0) {
            obj.numberUnavailable = Math.round(message.numberUnavailable);
        }
        if (message.collisionCount !== undefined && message.collisionCount !== 0) {
            obj.collisionCount = Math.round(message.collisionCount);
        }
        if (message.conditions?.length) {
            obj.conditions = message.conditions.map((e) => DaemonSetCondition.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DaemonSetStatus>, I>>(base?: I): DaemonSetStatus {
        return DaemonSetStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DaemonSetStatus>, I>>(object: I): DaemonSetStatus {
        const message = createBaseDaemonSetStatus();
        message.currentNumberScheduled = object.currentNumberScheduled ?? 0;
        message.numberMisscheduled = object.numberMisscheduled ?? 0;
        message.desiredNumberScheduled = object.desiredNumberScheduled ?? 0;
        message.numberReady = object.numberReady ?? 0;
        message.observedGeneration = object.observedGeneration ?? 0;
        message.updatedNumberScheduled = object.updatedNumberScheduled ?? 0;
        message.numberAvailable = object.numberAvailable ?? 0;
        message.numberUnavailable = object.numberUnavailable ?? 0;
        message.collisionCount = object.collisionCount ?? 0;
        message.conditions = object.conditions?.map((e) => DaemonSetCondition.fromPartial(e)) || [];
        return message;
    },
};

function createBaseDaemonSetUpdateStrategy(): DaemonSetUpdateStrategy {
    return { type: '', rollingUpdate: undefined };
}

export const DaemonSetUpdateStrategy: MessageFns<DaemonSetUpdateStrategy> = {
    encode(message: DaemonSetUpdateStrategy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.type !== undefined && message.type !== '') {
            writer.uint32(10).string(message.type);
        }
        if (message.rollingUpdate !== undefined) {
            RollingUpdateDaemonSet.encode(message.rollingUpdate, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DaemonSetUpdateStrategy {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDaemonSetUpdateStrategy();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.type = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.rollingUpdate = RollingUpdateDaemonSet.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DaemonSetUpdateStrategy {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : '',
            rollingUpdate: isSet(object.rollingUpdate)
                ? RollingUpdateDaemonSet.fromJSON(object.rollingUpdate)
                : undefined,
        };
    },

    toJSON(message: DaemonSetUpdateStrategy): unknown {
        const obj: any = {};
        if (message.type !== undefined && message.type !== '') {
            obj.type = message.type;
        }
        if (message.rollingUpdate !== undefined) {
            obj.rollingUpdate = RollingUpdateDaemonSet.toJSON(message.rollingUpdate);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DaemonSetUpdateStrategy>, I>>(base?: I): DaemonSetUpdateStrategy {
        return DaemonSetUpdateStrategy.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DaemonSetUpdateStrategy>, I>>(
        object: I,
    ): DaemonSetUpdateStrategy {
        const message = createBaseDaemonSetUpdateStrategy();
        message.type = object.type ?? '';
        message.rollingUpdate =
            object.rollingUpdate !== undefined && object.rollingUpdate !== null
                ? RollingUpdateDaemonSet.fromPartial(object.rollingUpdate)
                : undefined;
        return message;
    },
};

function createBaseDeployment(): Deployment {
    return { metadata: undefined, spec: undefined, status: undefined };
}

export const Deployment: MessageFns<Deployment> = {
    encode(message: Deployment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.spec !== undefined) {
            DeploymentSpec.encode(message.spec, writer.uint32(18).fork()).join();
        }
        if (message.status !== undefined) {
            DeploymentStatus.encode(message.status, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Deployment {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeployment();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.spec = DeploymentSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.status = DeploymentStatus.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Deployment {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            spec: isSet(object.spec) ? DeploymentSpec.fromJSON(object.spec) : undefined,
            status: isSet(object.status) ? DeploymentStatus.fromJSON(object.status) : undefined,
        };
    },

    toJSON(message: Deployment): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.spec !== undefined) {
            obj.spec = DeploymentSpec.toJSON(message.spec);
        }
        if (message.status !== undefined) {
            obj.status = DeploymentStatus.toJSON(message.status);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Deployment>, I>>(base?: I): Deployment {
        return Deployment.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Deployment>, I>>(object: I): Deployment {
        const message = createBaseDeployment();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.spec =
            object.spec !== undefined && object.spec !== null
                ? DeploymentSpec.fromPartial(object.spec)
                : undefined;
        message.status =
            object.status !== undefined && object.status !== null
                ? DeploymentStatus.fromPartial(object.status)
                : undefined;
        return message;
    },
};

function createBaseDeploymentCondition(): DeploymentCondition {
    return {
        type: '',
        status: '',
        lastUpdateTime: undefined,
        lastTransitionTime: undefined,
        reason: '',
        message: '',
    };
}

export const DeploymentCondition: MessageFns<DeploymentCondition> = {
    encode(message: DeploymentCondition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.type !== undefined && message.type !== '') {
            writer.uint32(10).string(message.type);
        }
        if (message.status !== undefined && message.status !== '') {
            writer.uint32(18).string(message.status);
        }
        if (message.lastUpdateTime !== undefined) {
            Time.encode(message.lastUpdateTime, writer.uint32(50).fork()).join();
        }
        if (message.lastTransitionTime !== undefined) {
            Time.encode(message.lastTransitionTime, writer.uint32(58).fork()).join();
        }
        if (message.reason !== undefined && message.reason !== '') {
            writer.uint32(34).string(message.reason);
        }
        if (message.message !== undefined && message.message !== '') {
            writer.uint32(42).string(message.message);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DeploymentCondition {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeploymentCondition();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.type = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.status = reader.string();
                        continue;
                    }
                    case 6: {
                        if (tag !== 50) {
                            break;
                        }

                        message.lastUpdateTime = Time.decode(reader, reader.uint32());
                        continue;
                    }
                    case 7: {
                        if (tag !== 58) {
                            break;
                        }

                        message.lastTransitionTime = Time.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.reason = reader.string();
                        continue;
                    }
                    case 5: {
                        if (tag !== 42) {
                            break;
                        }

                        message.message = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DeploymentCondition {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : '',
            status: isSet(object.status) ? globalThis.String(object.status) : '',
            lastUpdateTime: isSet(object.lastUpdateTime) ? Time.fromJSON(object.lastUpdateTime) : undefined,
            lastTransitionTime: isSet(object.lastTransitionTime)
                ? Time.fromJSON(object.lastTransitionTime)
                : undefined,
            reason: isSet(object.reason) ? globalThis.String(object.reason) : '',
            message: isSet(object.message) ? globalThis.String(object.message) : '',
        };
    },

    toJSON(message: DeploymentCondition): unknown {
        const obj: any = {};
        if (message.type !== undefined && message.type !== '') {
            obj.type = message.type;
        }
        if (message.status !== undefined && message.status !== '') {
            obj.status = message.status;
        }
        if (message.lastUpdateTime !== undefined) {
            obj.lastUpdateTime = Time.toJSON(message.lastUpdateTime);
        }
        if (message.lastTransitionTime !== undefined) {
            obj.lastTransitionTime = Time.toJSON(message.lastTransitionTime);
        }
        if (message.reason !== undefined && message.reason !== '') {
            obj.reason = message.reason;
        }
        if (message.message !== undefined && message.message !== '') {
            obj.message = message.message;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DeploymentCondition>, I>>(base?: I): DeploymentCondition {
        return DeploymentCondition.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DeploymentCondition>, I>>(object: I): DeploymentCondition {
        const message = createBaseDeploymentCondition();
        message.type = object.type ?? '';
        message.status = object.status ?? '';
        message.lastUpdateTime =
            object.lastUpdateTime !== undefined && object.lastUpdateTime !== null
                ? Time.fromPartial(object.lastUpdateTime)
                : undefined;
        message.lastTransitionTime =
            object.lastTransitionTime !== undefined && object.lastTransitionTime !== null
                ? Time.fromPartial(object.lastTransitionTime)
                : undefined;
        message.reason = object.reason ?? '';
        message.message = object.message ?? '';
        return message;
    },
};

function createBaseDeploymentList(): DeploymentList {
    return { metadata: undefined, items: [] };
}

export const DeploymentList: MessageFns<DeploymentList> = {
    encode(message: DeploymentList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            Deployment.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DeploymentList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeploymentList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(Deployment.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DeploymentList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => Deployment.fromJSON(e))
                : [],
        };
    },

    toJSON(message: DeploymentList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => Deployment.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DeploymentList>, I>>(base?: I): DeploymentList {
        return DeploymentList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DeploymentList>, I>>(object: I): DeploymentList {
        const message = createBaseDeploymentList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => Deployment.fromPartial(e)) || [];
        return message;
    },
};

function createBaseDeploymentRollback(): DeploymentRollback {
    return { name: '', updatedAnnotations: {}, rollbackTo: undefined };
}

export const DeploymentRollback: MessageFns<DeploymentRollback> = {
    encode(message: DeploymentRollback, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.name !== undefined && message.name !== '') {
            writer.uint32(10).string(message.name);
        }
        globalThis.Object.entries(message.updatedAnnotations).forEach(([key, value]: [string, string]) => {
            DeploymentRollback_UpdatedAnnotationsEntry.encode(
                { key: key as any, value },
                writer.uint32(18).fork(),
            ).join();
        });
        if (message.rollbackTo !== undefined) {
            RollbackConfig.encode(message.rollbackTo, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DeploymentRollback {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeploymentRollback();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.name = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        const entry2 = DeploymentRollback_UpdatedAnnotationsEntry.decode(
                            reader,
                            reader.uint32(),
                        );
                        if (entry2.value !== undefined) {
                            message.updatedAnnotations[entry2.key] = entry2.value;
                        }
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.rollbackTo = RollbackConfig.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DeploymentRollback {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : '',
            updatedAnnotations: isObject(object.updatedAnnotations)
                ? (globalThis.Object.entries(object.updatedAnnotations) as [string, any][]).reduce(
                      (acc: { [key: string]: string }, [key, value]: [string, any]) => {
                          globalThis.Object.defineProperty(acc, key, {
                              value: globalThis.String(value),
                              enumerable: true,
                              configurable: true,
                              writable: true,
                          });
                          return acc;
                      },
                      {},
                  )
                : {},
            rollbackTo: isSet(object.rollbackTo) ? RollbackConfig.fromJSON(object.rollbackTo) : undefined,
        };
    },

    toJSON(message: DeploymentRollback): unknown {
        const obj: any = {};
        if (message.name !== undefined && message.name !== '') {
            obj.name = message.name;
        }
        if (message.updatedAnnotations) {
            const entries = globalThis.Object.entries(message.updatedAnnotations) as [string, string][];
            if (entries.length > 0) {
                obj.updatedAnnotations = {};
                entries.forEach(([k, v]) => {
                    obj.updatedAnnotations[k] = v;
                });
            }
        }
        if (message.rollbackTo !== undefined) {
            obj.rollbackTo = RollbackConfig.toJSON(message.rollbackTo);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DeploymentRollback>, I>>(base?: I): DeploymentRollback {
        return DeploymentRollback.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DeploymentRollback>, I>>(object: I): DeploymentRollback {
        const message = createBaseDeploymentRollback();
        message.name = object.name ?? '';
        message.updatedAnnotations = (
            globalThis.Object.entries(object.updatedAnnotations ?? {}) as [string, string][]
        ).reduce((acc: { [key: string]: string }, [key, value]: [string, string]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        message.rollbackTo =
            object.rollbackTo !== undefined && object.rollbackTo !== null
                ? RollbackConfig.fromPartial(object.rollbackTo)
                : undefined;
        return message;
    },
};

function createBaseDeploymentRollback_UpdatedAnnotationsEntry(): DeploymentRollback_UpdatedAnnotationsEntry {
    return { key: '', value: '' };
}

export const DeploymentRollback_UpdatedAnnotationsEntry: MessageFns<DeploymentRollback_UpdatedAnnotationsEntry> =
    {
        encode(
            message: DeploymentRollback_UpdatedAnnotationsEntry,
            writer: BinaryWriter = new BinaryWriter(),
        ): BinaryWriter {
            if (message.key !== '') {
                writer.uint32(10).string(message.key);
            }
            if (message.value !== '') {
                writer.uint32(18).string(message.value);
            }
            return writer;
        },

        decode(
            input: BinaryReader | Uint8Array,
            length?: number,
        ): DeploymentRollback_UpdatedAnnotationsEntry {
            const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
            const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
            if (previousRecursionDepth >= 100) {
                throw new globalThis.Error('protobuf decode recursion limit exceeded');
            }
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
            try {
                const end = length === undefined ? reader.len : reader.pos + length;
                const message = createBaseDeploymentRollback_UpdatedAnnotationsEntry();
                while (reader.pos < end) {
                    const tag = reader.uint32();
                    switch (tag >>> 3) {
                        case 1: {
                            if (tag !== 10) {
                                break;
                            }

                            message.key = reader.string();
                            continue;
                        }
                        case 2: {
                            if (tag !== 18) {
                                break;
                            }

                            message.value = reader.string();
                            continue;
                        }
                    }
                    if ((tag & 7) === 4 || tag === 0) {
                        break;
                    }
                    reader.skip(tag & 7);
                }
                return message;
            } finally {
                (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
            }
        },

        fromJSON(object: any): DeploymentRollback_UpdatedAnnotationsEntry {
            return {
                key: isSet(object.key) ? globalThis.String(object.key) : '',
                value: isSet(object.value) ? globalThis.String(object.value) : '',
            };
        },

        toJSON(message: DeploymentRollback_UpdatedAnnotationsEntry): unknown {
            const obj: any = {};
            if (message.key !== '') {
                obj.key = message.key;
            }
            if (message.value !== '') {
                obj.value = message.value;
            }
            return obj;
        },

        create<I extends Exact<DeepPartial<DeploymentRollback_UpdatedAnnotationsEntry>, I>>(
            base?: I,
        ): DeploymentRollback_UpdatedAnnotationsEntry {
            return DeploymentRollback_UpdatedAnnotationsEntry.fromPartial(base ?? ({} as any));
        },
        fromPartial<I extends Exact<DeepPartial<DeploymentRollback_UpdatedAnnotationsEntry>, I>>(
            object: I,
        ): DeploymentRollback_UpdatedAnnotationsEntry {
            const message = createBaseDeploymentRollback_UpdatedAnnotationsEntry();
            message.key = object.key ?? '';
            message.value = object.value ?? '';
            return message;
        },
    };

function createBaseDeploymentSpec(): DeploymentSpec {
    return {
        replicas: 0,
        selector: undefined,
        template: undefined,
        strategy: undefined,
        minReadySeconds: 0,
        revisionHistoryLimit: 0,
        paused: false,
        rollbackTo: undefined,
        progressDeadlineSeconds: 0,
    };
}

export const DeploymentSpec: MessageFns<DeploymentSpec> = {
    encode(message: DeploymentSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.replicas !== undefined && message.replicas !== 0) {
            writer.uint32(8).int32(message.replicas);
        }
        if (message.selector !== undefined) {
            LabelSelector.encode(message.selector, writer.uint32(18).fork()).join();
        }
        if (message.template !== undefined) {
            PodTemplateSpec.encode(message.template, writer.uint32(26).fork()).join();
        }
        if (message.strategy !== undefined) {
            DeploymentStrategy.encode(message.strategy, writer.uint32(34).fork()).join();
        }
        if (message.minReadySeconds !== undefined && message.minReadySeconds !== 0) {
            writer.uint32(40).int32(message.minReadySeconds);
        }
        if (message.revisionHistoryLimit !== undefined && message.revisionHistoryLimit !== 0) {
            writer.uint32(48).int32(message.revisionHistoryLimit);
        }
        if (message.paused !== undefined && message.paused !== false) {
            writer.uint32(56).bool(message.paused);
        }
        if (message.rollbackTo !== undefined) {
            RollbackConfig.encode(message.rollbackTo, writer.uint32(66).fork()).join();
        }
        if (message.progressDeadlineSeconds !== undefined && message.progressDeadlineSeconds !== 0) {
            writer.uint32(72).int32(message.progressDeadlineSeconds);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DeploymentSpec {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeploymentSpec();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.replicas = reader.int32();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.selector = LabelSelector.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.template = PodTemplateSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.strategy = DeploymentStrategy.decode(reader, reader.uint32());
                        continue;
                    }
                    case 5: {
                        if (tag !== 40) {
                            break;
                        }

                        message.minReadySeconds = reader.int32();
                        continue;
                    }
                    case 6: {
                        if (tag !== 48) {
                            break;
                        }

                        message.revisionHistoryLimit = reader.int32();
                        continue;
                    }
                    case 7: {
                        if (tag !== 56) {
                            break;
                        }

                        message.paused = reader.bool();
                        continue;
                    }
                    case 8: {
                        if (tag !== 66) {
                            break;
                        }

                        message.rollbackTo = RollbackConfig.decode(reader, reader.uint32());
                        continue;
                    }
                    case 9: {
                        if (tag !== 72) {
                            break;
                        }

                        message.progressDeadlineSeconds = reader.int32();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DeploymentSpec {
        return {
            replicas: isSet(object.replicas) ? globalThis.Number(object.replicas) : 0,
            selector: isSet(object.selector) ? LabelSelector.fromJSON(object.selector) : undefined,
            template: isSet(object.template) ? PodTemplateSpec.fromJSON(object.template) : undefined,
            strategy: isSet(object.strategy) ? DeploymentStrategy.fromJSON(object.strategy) : undefined,
            minReadySeconds: isSet(object.minReadySeconds) ? globalThis.Number(object.minReadySeconds) : 0,
            revisionHistoryLimit: isSet(object.revisionHistoryLimit)
                ? globalThis.Number(object.revisionHistoryLimit)
                : 0,
            paused: isSet(object.paused) ? globalThis.Boolean(object.paused) : false,
            rollbackTo: isSet(object.rollbackTo) ? RollbackConfig.fromJSON(object.rollbackTo) : undefined,
            progressDeadlineSeconds: isSet(object.progressDeadlineSeconds)
                ? globalThis.Number(object.progressDeadlineSeconds)
                : 0,
        };
    },

    toJSON(message: DeploymentSpec): unknown {
        const obj: any = {};
        if (message.replicas !== undefined && message.replicas !== 0) {
            obj.replicas = Math.round(message.replicas);
        }
        if (message.selector !== undefined) {
            obj.selector = LabelSelector.toJSON(message.selector);
        }
        if (message.template !== undefined) {
            obj.template = PodTemplateSpec.toJSON(message.template);
        }
        if (message.strategy !== undefined) {
            obj.strategy = DeploymentStrategy.toJSON(message.strategy);
        }
        if (message.minReadySeconds !== undefined && message.minReadySeconds !== 0) {
            obj.minReadySeconds = Math.round(message.minReadySeconds);
        }
        if (message.revisionHistoryLimit !== undefined && message.revisionHistoryLimit !== 0) {
            obj.revisionHistoryLimit = Math.round(message.revisionHistoryLimit);
        }
        if (message.paused !== undefined && message.paused !== false) {
            obj.paused = message.paused;
        }
        if (message.rollbackTo !== undefined) {
            obj.rollbackTo = RollbackConfig.toJSON(message.rollbackTo);
        }
        if (message.progressDeadlineSeconds !== undefined && message.progressDeadlineSeconds !== 0) {
            obj.progressDeadlineSeconds = Math.round(message.progressDeadlineSeconds);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DeploymentSpec>, I>>(base?: I): DeploymentSpec {
        return DeploymentSpec.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DeploymentSpec>, I>>(object: I): DeploymentSpec {
        const message = createBaseDeploymentSpec();
        message.replicas = object.replicas ?? 0;
        message.selector =
            object.selector !== undefined && object.selector !== null
                ? LabelSelector.fromPartial(object.selector)
                : undefined;
        message.template =
            object.template !== undefined && object.template !== null
                ? PodTemplateSpec.fromPartial(object.template)
                : undefined;
        message.strategy =
            object.strategy !== undefined && object.strategy !== null
                ? DeploymentStrategy.fromPartial(object.strategy)
                : undefined;
        message.minReadySeconds = object.minReadySeconds ?? 0;
        message.revisionHistoryLimit = object.revisionHistoryLimit ?? 0;
        message.paused = object.paused ?? false;
        message.rollbackTo =
            object.rollbackTo !== undefined && object.rollbackTo !== null
                ? RollbackConfig.fromPartial(object.rollbackTo)
                : undefined;
        message.progressDeadlineSeconds = object.progressDeadlineSeconds ?? 0;
        return message;
    },
};

function createBaseDeploymentStatus(): DeploymentStatus {
    return {
        observedGeneration: 0,
        replicas: 0,
        updatedReplicas: 0,
        readyReplicas: 0,
        availableReplicas: 0,
        unavailableReplicas: 0,
        terminatingReplicas: 0,
        conditions: [],
        collisionCount: 0,
    };
}

export const DeploymentStatus: MessageFns<DeploymentStatus> = {
    encode(message: DeploymentStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.observedGeneration !== undefined && message.observedGeneration !== 0) {
            writer.uint32(8).int64(message.observedGeneration);
        }
        if (message.replicas !== undefined && message.replicas !== 0) {
            writer.uint32(16).int32(message.replicas);
        }
        if (message.updatedReplicas !== undefined && message.updatedReplicas !== 0) {
            writer.uint32(24).int32(message.updatedReplicas);
        }
        if (message.readyReplicas !== undefined && message.readyReplicas !== 0) {
            writer.uint32(56).int32(message.readyReplicas);
        }
        if (message.availableReplicas !== undefined && message.availableReplicas !== 0) {
            writer.uint32(32).int32(message.availableReplicas);
        }
        if (message.unavailableReplicas !== undefined && message.unavailableReplicas !== 0) {
            writer.uint32(40).int32(message.unavailableReplicas);
        }
        if (message.terminatingReplicas !== undefined && message.terminatingReplicas !== 0) {
            writer.uint32(72).int32(message.terminatingReplicas);
        }
        for (const v of message.conditions) {
            DeploymentCondition.encode(v!, writer.uint32(50).fork()).join();
        }
        if (message.collisionCount !== undefined && message.collisionCount !== 0) {
            writer.uint32(64).int32(message.collisionCount);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DeploymentStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeploymentStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.observedGeneration = longToNumber(reader.int64());
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }

                        message.replicas = reader.int32();
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }

                        message.updatedReplicas = reader.int32();
                        continue;
                    }
                    case 7: {
                        if (tag !== 56) {
                            break;
                        }

                        message.readyReplicas = reader.int32();
                        continue;
                    }
                    case 4: {
                        if (tag !== 32) {
                            break;
                        }

                        message.availableReplicas = reader.int32();
                        continue;
                    }
                    case 5: {
                        if (tag !== 40) {
                            break;
                        }

                        message.unavailableReplicas = reader.int32();
                        continue;
                    }
                    case 9: {
                        if (tag !== 72) {
                            break;
                        }

                        message.terminatingReplicas = reader.int32();
                        continue;
                    }
                    case 6: {
                        if (tag !== 50) {
                            break;
                        }

                        message.conditions.push(DeploymentCondition.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 8: {
                        if (tag !== 64) {
                            break;
                        }

                        message.collisionCount = reader.int32();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DeploymentStatus {
        return {
            observedGeneration: isSet(object.observedGeneration)
                ? globalThis.Number(object.observedGeneration)
                : 0,
            replicas: isSet(object.replicas) ? globalThis.Number(object.replicas) : 0,
            updatedReplicas: isSet(object.updatedReplicas) ? globalThis.Number(object.updatedReplicas) : 0,
            readyReplicas: isSet(object.readyReplicas) ? globalThis.Number(object.readyReplicas) : 0,
            availableReplicas: isSet(object.availableReplicas)
                ? globalThis.Number(object.availableReplicas)
                : 0,
            unavailableReplicas: isSet(object.unavailableReplicas)
                ? globalThis.Number(object.unavailableReplicas)
                : 0,
            terminatingReplicas: isSet(object.terminatingReplicas)
                ? globalThis.Number(object.terminatingReplicas)
                : 0,
            conditions: globalThis.Array.isArray(object?.conditions)
                ? object.conditions.map((e: any) => DeploymentCondition.fromJSON(e))
                : [],
            collisionCount: isSet(object.collisionCount) ? globalThis.Number(object.collisionCount) : 0,
        };
    },

    toJSON(message: DeploymentStatus): unknown {
        const obj: any = {};
        if (message.observedGeneration !== undefined && message.observedGeneration !== 0) {
            obj.observedGeneration = Math.round(message.observedGeneration);
        }
        if (message.replicas !== undefined && message.replicas !== 0) {
            obj.replicas = Math.round(message.replicas);
        }
        if (message.updatedReplicas !== undefined && message.updatedReplicas !== 0) {
            obj.updatedReplicas = Math.round(message.updatedReplicas);
        }
        if (message.readyReplicas !== undefined && message.readyReplicas !== 0) {
            obj.readyReplicas = Math.round(message.readyReplicas);
        }
        if (message.availableReplicas !== undefined && message.availableReplicas !== 0) {
            obj.availableReplicas = Math.round(message.availableReplicas);
        }
        if (message.unavailableReplicas !== undefined && message.unavailableReplicas !== 0) {
            obj.unavailableReplicas = Math.round(message.unavailableReplicas);
        }
        if (message.terminatingReplicas !== undefined && message.terminatingReplicas !== 0) {
            obj.terminatingReplicas = Math.round(message.terminatingReplicas);
        }
        if (message.conditions?.length) {
            obj.conditions = message.conditions.map((e) => DeploymentCondition.toJSON(e));
        }
        if (message.collisionCount !== undefined && message.collisionCount !== 0) {
            obj.collisionCount = Math.round(message.collisionCount);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DeploymentStatus>, I>>(base?: I): DeploymentStatus {
        return DeploymentStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DeploymentStatus>, I>>(object: I): DeploymentStatus {
        const message = createBaseDeploymentStatus();
        message.observedGeneration = object.observedGeneration ?? 0;
        message.replicas = object.replicas ?? 0;
        message.updatedReplicas = object.updatedReplicas ?? 0;
        message.readyReplicas = object.readyReplicas ?? 0;
        message.availableReplicas = object.availableReplicas ?? 0;
        message.unavailableReplicas = object.unavailableReplicas ?? 0;
        message.terminatingReplicas = object.terminatingReplicas ?? 0;
        message.conditions = object.conditions?.map((e) => DeploymentCondition.fromPartial(e)) || [];
        message.collisionCount = object.collisionCount ?? 0;
        return message;
    },
};

function createBaseDeploymentStrategy(): DeploymentStrategy {
    return { type: '', rollingUpdate: undefined };
}

export const DeploymentStrategy: MessageFns<DeploymentStrategy> = {
    encode(message: DeploymentStrategy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.type !== undefined && message.type !== '') {
            writer.uint32(10).string(message.type);
        }
        if (message.rollingUpdate !== undefined) {
            RollingUpdateDeployment.encode(message.rollingUpdate, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): DeploymentStrategy {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseDeploymentStrategy();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.type = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.rollingUpdate = RollingUpdateDeployment.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): DeploymentStrategy {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : '',
            rollingUpdate: isSet(object.rollingUpdate)
                ? RollingUpdateDeployment.fromJSON(object.rollingUpdate)
                : undefined,
        };
    },

    toJSON(message: DeploymentStrategy): unknown {
        const obj: any = {};
        if (message.type !== undefined && message.type !== '') {
            obj.type = message.type;
        }
        if (message.rollingUpdate !== undefined) {
            obj.rollingUpdate = RollingUpdateDeployment.toJSON(message.rollingUpdate);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<DeploymentStrategy>, I>>(base?: I): DeploymentStrategy {
        return DeploymentStrategy.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<DeploymentStrategy>, I>>(object: I): DeploymentStrategy {
        const message = createBaseDeploymentStrategy();
        message.type = object.type ?? '';
        message.rollingUpdate =
            object.rollingUpdate !== undefined && object.rollingUpdate !== null
                ? RollingUpdateDeployment.fromPartial(object.rollingUpdate)
                : undefined;
        return message;
    },
};

function createBaseHTTPIngressPath(): HTTPIngressPath {
    return { path: '', pathType: '', backend: undefined };
}

export const HTTPIngressPath: MessageFns<HTTPIngressPath> = {
    encode(message: HTTPIngressPath, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.path !== undefined && message.path !== '') {
            writer.uint32(10).string(message.path);
        }
        if (message.pathType !== undefined && message.pathType !== '') {
            writer.uint32(26).string(message.pathType);
        }
        if (message.backend !== undefined) {
            IngressBackend.encode(message.backend, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): HTTPIngressPath {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseHTTPIngressPath();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.path = reader.string();
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.pathType = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.backend = IngressBackend.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): HTTPIngressPath {
        return {
            path: isSet(object.path) ? globalThis.String(object.path) : '',
            pathType: isSet(object.pathType) ? globalThis.String(object.pathType) : '',
            backend: isSet(object.backend) ? IngressBackend.fromJSON(object.backend) : undefined,
        };
    },

    toJSON(message: HTTPIngressPath): unknown {
        const obj: any = {};
        if (message.path !== undefined && message.path !== '') {
            obj.path = message.path;
        }
        if (message.pathType !== undefined && message.pathType !== '') {
            obj.pathType = message.pathType;
        }
        if (message.backend !== undefined) {
            obj.backend = IngressBackend.toJSON(message.backend);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<HTTPIngressPath>, I>>(base?: I): HTTPIngressPath {
        return HTTPIngressPath.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<HTTPIngressPath>, I>>(object: I): HTTPIngressPath {
        const message = createBaseHTTPIngressPath();
        message.path = object.path ?? '';
        message.pathType = object.pathType ?? '';
        message.backend =
            object.backend !== undefined && object.backend !== null
                ? IngressBackend.fromPartial(object.backend)
                : undefined;
        return message;
    },
};

function createBaseHTTPIngressRuleValue(): HTTPIngressRuleValue {
    return { paths: [] };
}

export const HTTPIngressRuleValue: MessageFns<HTTPIngressRuleValue> = {
    encode(message: HTTPIngressRuleValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.paths) {
            HTTPIngressPath.encode(v!, writer.uint32(10).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): HTTPIngressRuleValue {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseHTTPIngressRuleValue();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.paths.push(HTTPIngressPath.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): HTTPIngressRuleValue {
        return {
            paths: globalThis.Array.isArray(object?.paths)
                ? object.paths.map((e: any) => HTTPIngressPath.fromJSON(e))
                : [],
        };
    },

    toJSON(message: HTTPIngressRuleValue): unknown {
        const obj: any = {};
        if (message.paths?.length) {
            obj.paths = message.paths.map((e) => HTTPIngressPath.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<HTTPIngressRuleValue>, I>>(base?: I): HTTPIngressRuleValue {
        return HTTPIngressRuleValue.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<HTTPIngressRuleValue>, I>>(object: I): HTTPIngressRuleValue {
        const message = createBaseHTTPIngressRuleValue();
        message.paths = object.paths?.map((e) => HTTPIngressPath.fromPartial(e)) || [];
        return message;
    },
};

function createBaseIPBlock(): IPBlock {
    return { cidr: '', except: [] };
}

export const IPBlock: MessageFns<IPBlock> = {
    encode(message: IPBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.cidr !== undefined && message.cidr !== '') {
            writer.uint32(10).string(message.cidr);
        }
        for (const v of message.except) {
            writer.uint32(18).string(v!);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IPBlock {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIPBlock();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.cidr = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.except.push(reader.string());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IPBlock {
        return {
            cidr: isSet(object.cidr) ? globalThis.String(object.cidr) : '',
            except: globalThis.Array.isArray(object?.except)
                ? object.except.map((e: any) => globalThis.String(e))
                : [],
        };
    },

    toJSON(message: IPBlock): unknown {
        const obj: any = {};
        if (message.cidr !== undefined && message.cidr !== '') {
            obj.cidr = message.cidr;
        }
        if (message.except?.length) {
            obj.except = message.except;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IPBlock>, I>>(base?: I): IPBlock {
        return IPBlock.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IPBlock>, I>>(object: I): IPBlock {
        const message = createBaseIPBlock();
        message.cidr = object.cidr ?? '';
        message.except = object.except?.map((e) => e) || [];
        return message;
    },
};

function createBaseIngress(): Ingress {
    return { metadata: undefined, spec: undefined, status: undefined };
}

export const Ingress: MessageFns<Ingress> = {
    encode(message: Ingress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.spec !== undefined) {
            IngressSpec.encode(message.spec, writer.uint32(18).fork()).join();
        }
        if (message.status !== undefined) {
            IngressStatus.encode(message.status, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Ingress {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngress();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.spec = IngressSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.status = IngressStatus.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Ingress {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            spec: isSet(object.spec) ? IngressSpec.fromJSON(object.spec) : undefined,
            status: isSet(object.status) ? IngressStatus.fromJSON(object.status) : undefined,
        };
    },

    toJSON(message: Ingress): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.spec !== undefined) {
            obj.spec = IngressSpec.toJSON(message.spec);
        }
        if (message.status !== undefined) {
            obj.status = IngressStatus.toJSON(message.status);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Ingress>, I>>(base?: I): Ingress {
        return Ingress.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Ingress>, I>>(object: I): Ingress {
        const message = createBaseIngress();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.spec =
            object.spec !== undefined && object.spec !== null
                ? IngressSpec.fromPartial(object.spec)
                : undefined;
        message.status =
            object.status !== undefined && object.status !== null
                ? IngressStatus.fromPartial(object.status)
                : undefined;
        return message;
    },
};

function createBaseIngressBackend(): IngressBackend {
    return { serviceName: '', servicePort: undefined, resource: undefined };
}

export const IngressBackend: MessageFns<IngressBackend> = {
    encode(message: IngressBackend, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.serviceName !== undefined && message.serviceName !== '') {
            writer.uint32(10).string(message.serviceName);
        }
        if (message.servicePort !== undefined) {
            IntOrString.encode(message.servicePort, writer.uint32(18).fork()).join();
        }
        if (message.resource !== undefined) {
            TypedLocalObjectReference.encode(message.resource, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressBackend {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressBackend();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.serviceName = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.servicePort = IntOrString.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.resource = TypedLocalObjectReference.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressBackend {
        return {
            serviceName: isSet(object.serviceName) ? globalThis.String(object.serviceName) : '',
            servicePort: isSet(object.servicePort) ? IntOrString.fromJSON(object.servicePort) : undefined,
            resource: isSet(object.resource)
                ? TypedLocalObjectReference.fromJSON(object.resource)
                : undefined,
        };
    },

    toJSON(message: IngressBackend): unknown {
        const obj: any = {};
        if (message.serviceName !== undefined && message.serviceName !== '') {
            obj.serviceName = message.serviceName;
        }
        if (message.servicePort !== undefined) {
            obj.servicePort = IntOrString.toJSON(message.servicePort);
        }
        if (message.resource !== undefined) {
            obj.resource = TypedLocalObjectReference.toJSON(message.resource);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressBackend>, I>>(base?: I): IngressBackend {
        return IngressBackend.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressBackend>, I>>(object: I): IngressBackend {
        const message = createBaseIngressBackend();
        message.serviceName = object.serviceName ?? '';
        message.servicePort =
            object.servicePort !== undefined && object.servicePort !== null
                ? IntOrString.fromPartial(object.servicePort)
                : undefined;
        message.resource =
            object.resource !== undefined && object.resource !== null
                ? TypedLocalObjectReference.fromPartial(object.resource)
                : undefined;
        return message;
    },
};

function createBaseIngressList(): IngressList {
    return { metadata: undefined, items: [] };
}

export const IngressList: MessageFns<IngressList> = {
    encode(message: IngressList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            Ingress.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(Ingress.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => Ingress.fromJSON(e))
                : [],
        };
    },

    toJSON(message: IngressList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => Ingress.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressList>, I>>(base?: I): IngressList {
        return IngressList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressList>, I>>(object: I): IngressList {
        const message = createBaseIngressList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => Ingress.fromPartial(e)) || [];
        return message;
    },
};

function createBaseIngressLoadBalancerIngress(): IngressLoadBalancerIngress {
    return { ip: '', hostname: '', ports: [] };
}

export const IngressLoadBalancerIngress: MessageFns<IngressLoadBalancerIngress> = {
    encode(message: IngressLoadBalancerIngress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.ip !== undefined && message.ip !== '') {
            writer.uint32(10).string(message.ip);
        }
        if (message.hostname !== undefined && message.hostname !== '') {
            writer.uint32(18).string(message.hostname);
        }
        for (const v of message.ports) {
            IngressPortStatus.encode(v!, writer.uint32(34).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressLoadBalancerIngress {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressLoadBalancerIngress();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.ip = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.hostname = reader.string();
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.ports.push(IngressPortStatus.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressLoadBalancerIngress {
        return {
            ip: isSet(object.ip) ? globalThis.String(object.ip) : '',
            hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : '',
            ports: globalThis.Array.isArray(object?.ports)
                ? object.ports.map((e: any) => IngressPortStatus.fromJSON(e))
                : [],
        };
    },

    toJSON(message: IngressLoadBalancerIngress): unknown {
        const obj: any = {};
        if (message.ip !== undefined && message.ip !== '') {
            obj.ip = message.ip;
        }
        if (message.hostname !== undefined && message.hostname !== '') {
            obj.hostname = message.hostname;
        }
        if (message.ports?.length) {
            obj.ports = message.ports.map((e) => IngressPortStatus.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressLoadBalancerIngress>, I>>(
        base?: I,
    ): IngressLoadBalancerIngress {
        return IngressLoadBalancerIngress.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressLoadBalancerIngress>, I>>(
        object: I,
    ): IngressLoadBalancerIngress {
        const message = createBaseIngressLoadBalancerIngress();
        message.ip = object.ip ?? '';
        message.hostname = object.hostname ?? '';
        message.ports = object.ports?.map((e) => IngressPortStatus.fromPartial(e)) || [];
        return message;
    },
};

function createBaseIngressLoadBalancerStatus(): IngressLoadBalancerStatus {
    return { ingress: [] };
}

export const IngressLoadBalancerStatus: MessageFns<IngressLoadBalancerStatus> = {
    encode(message: IngressLoadBalancerStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.ingress) {
            IngressLoadBalancerIngress.encode(v!, writer.uint32(10).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressLoadBalancerStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressLoadBalancerStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.ingress.push(IngressLoadBalancerIngress.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressLoadBalancerStatus {
        return {
            ingress: globalThis.Array.isArray(object?.ingress)
                ? object.ingress.map((e: any) => IngressLoadBalancerIngress.fromJSON(e))
                : [],
        };
    },

    toJSON(message: IngressLoadBalancerStatus): unknown {
        const obj: any = {};
        if (message.ingress?.length) {
            obj.ingress = message.ingress.map((e) => IngressLoadBalancerIngress.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressLoadBalancerStatus>, I>>(base?: I): IngressLoadBalancerStatus {
        return IngressLoadBalancerStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressLoadBalancerStatus>, I>>(
        object: I,
    ): IngressLoadBalancerStatus {
        const message = createBaseIngressLoadBalancerStatus();
        message.ingress = object.ingress?.map((e) => IngressLoadBalancerIngress.fromPartial(e)) || [];
        return message;
    },
};

function createBaseIngressPortStatus(): IngressPortStatus {
    return { port: 0, protocol: '', error: '' };
}

export const IngressPortStatus: MessageFns<IngressPortStatus> = {
    encode(message: IngressPortStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.port !== undefined && message.port !== 0) {
            writer.uint32(8).int32(message.port);
        }
        if (message.protocol !== undefined && message.protocol !== '') {
            writer.uint32(18).string(message.protocol);
        }
        if (message.error !== undefined && message.error !== '') {
            writer.uint32(26).string(message.error);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressPortStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressPortStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.port = reader.int32();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.protocol = reader.string();
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.error = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressPortStatus {
        return {
            port: isSet(object.port) ? globalThis.Number(object.port) : 0,
            protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : '',
            error: isSet(object.error) ? globalThis.String(object.error) : '',
        };
    },

    toJSON(message: IngressPortStatus): unknown {
        const obj: any = {};
        if (message.port !== undefined && message.port !== 0) {
            obj.port = Math.round(message.port);
        }
        if (message.protocol !== undefined && message.protocol !== '') {
            obj.protocol = message.protocol;
        }
        if (message.error !== undefined && message.error !== '') {
            obj.error = message.error;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressPortStatus>, I>>(base?: I): IngressPortStatus {
        return IngressPortStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressPortStatus>, I>>(object: I): IngressPortStatus {
        const message = createBaseIngressPortStatus();
        message.port = object.port ?? 0;
        message.protocol = object.protocol ?? '';
        message.error = object.error ?? '';
        return message;
    },
};

function createBaseIngressRule(): IngressRule {
    return { host: '', ingressRuleValue: undefined };
}

export const IngressRule: MessageFns<IngressRule> = {
    encode(message: IngressRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.host !== undefined && message.host !== '') {
            writer.uint32(10).string(message.host);
        }
        if (message.ingressRuleValue !== undefined) {
            IngressRuleValue.encode(message.ingressRuleValue, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressRule {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressRule();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.host = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.ingressRuleValue = IngressRuleValue.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressRule {
        return {
            host: isSet(object.host) ? globalThis.String(object.host) : '',
            ingressRuleValue: isSet(object.ingressRuleValue)
                ? IngressRuleValue.fromJSON(object.ingressRuleValue)
                : undefined,
        };
    },

    toJSON(message: IngressRule): unknown {
        const obj: any = {};
        if (message.host !== undefined && message.host !== '') {
            obj.host = message.host;
        }
        if (message.ingressRuleValue !== undefined) {
            obj.ingressRuleValue = IngressRuleValue.toJSON(message.ingressRuleValue);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressRule>, I>>(base?: I): IngressRule {
        return IngressRule.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressRule>, I>>(object: I): IngressRule {
        const message = createBaseIngressRule();
        message.host = object.host ?? '';
        message.ingressRuleValue =
            object.ingressRuleValue !== undefined && object.ingressRuleValue !== null
                ? IngressRuleValue.fromPartial(object.ingressRuleValue)
                : undefined;
        return message;
    },
};

function createBaseIngressRuleValue(): IngressRuleValue {
    return { http: undefined };
}

export const IngressRuleValue: MessageFns<IngressRuleValue> = {
    encode(message: IngressRuleValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.http !== undefined) {
            HTTPIngressRuleValue.encode(message.http, writer.uint32(10).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressRuleValue {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressRuleValue();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.http = HTTPIngressRuleValue.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressRuleValue {
        return { http: isSet(object.http) ? HTTPIngressRuleValue.fromJSON(object.http) : undefined };
    },

    toJSON(message: IngressRuleValue): unknown {
        const obj: any = {};
        if (message.http !== undefined) {
            obj.http = HTTPIngressRuleValue.toJSON(message.http);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressRuleValue>, I>>(base?: I): IngressRuleValue {
        return IngressRuleValue.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressRuleValue>, I>>(object: I): IngressRuleValue {
        const message = createBaseIngressRuleValue();
        message.http =
            object.http !== undefined && object.http !== null
                ? HTTPIngressRuleValue.fromPartial(object.http)
                : undefined;
        return message;
    },
};

function createBaseIngressSpec(): IngressSpec {
    return { ingressClassName: '', backend: undefined, tls: [], rules: [] };
}

export const IngressSpec: MessageFns<IngressSpec> = {
    encode(message: IngressSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.ingressClassName !== undefined && message.ingressClassName !== '') {
            writer.uint32(34).string(message.ingressClassName);
        }
        if (message.backend !== undefined) {
            IngressBackend.encode(message.backend, writer.uint32(10).fork()).join();
        }
        for (const v of message.tls) {
            IngressTLS.encode(v!, writer.uint32(18).fork()).join();
        }
        for (const v of message.rules) {
            IngressRule.encode(v!, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressSpec {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressSpec();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.ingressClassName = reader.string();
                        continue;
                    }
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.backend = IngressBackend.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.tls.push(IngressTLS.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.rules.push(IngressRule.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressSpec {
        return {
            ingressClassName: isSet(object.ingressClassName)
                ? globalThis.String(object.ingressClassName)
                : '',
            backend: isSet(object.backend) ? IngressBackend.fromJSON(object.backend) : undefined,
            tls: globalThis.Array.isArray(object?.tls)
                ? object.tls.map((e: any) => IngressTLS.fromJSON(e))
                : [],
            rules: globalThis.Array.isArray(object?.rules)
                ? object.rules.map((e: any) => IngressRule.fromJSON(e))
                : [],
        };
    },

    toJSON(message: IngressSpec): unknown {
        const obj: any = {};
        if (message.ingressClassName !== undefined && message.ingressClassName !== '') {
            obj.ingressClassName = message.ingressClassName;
        }
        if (message.backend !== undefined) {
            obj.backend = IngressBackend.toJSON(message.backend);
        }
        if (message.tls?.length) {
            obj.tls = message.tls.map((e) => IngressTLS.toJSON(e));
        }
        if (message.rules?.length) {
            obj.rules = message.rules.map((e) => IngressRule.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressSpec>, I>>(base?: I): IngressSpec {
        return IngressSpec.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressSpec>, I>>(object: I): IngressSpec {
        const message = createBaseIngressSpec();
        message.ingressClassName = object.ingressClassName ?? '';
        message.backend =
            object.backend !== undefined && object.backend !== null
                ? IngressBackend.fromPartial(object.backend)
                : undefined;
        message.tls = object.tls?.map((e) => IngressTLS.fromPartial(e)) || [];
        message.rules = object.rules?.map((e) => IngressRule.fromPartial(e)) || [];
        return message;
    },
};

function createBaseIngressStatus(): IngressStatus {
    return { loadBalancer: undefined };
}

export const IngressStatus: MessageFns<IngressStatus> = {
    encode(message: IngressStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.loadBalancer !== undefined) {
            IngressLoadBalancerStatus.encode(message.loadBalancer, writer.uint32(10).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.loadBalancer = IngressLoadBalancerStatus.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressStatus {
        return {
            loadBalancer: isSet(object.loadBalancer)
                ? IngressLoadBalancerStatus.fromJSON(object.loadBalancer)
                : undefined,
        };
    },

    toJSON(message: IngressStatus): unknown {
        const obj: any = {};
        if (message.loadBalancer !== undefined) {
            obj.loadBalancer = IngressLoadBalancerStatus.toJSON(message.loadBalancer);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressStatus>, I>>(base?: I): IngressStatus {
        return IngressStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressStatus>, I>>(object: I): IngressStatus {
        const message = createBaseIngressStatus();
        message.loadBalancer =
            object.loadBalancer !== undefined && object.loadBalancer !== null
                ? IngressLoadBalancerStatus.fromPartial(object.loadBalancer)
                : undefined;
        return message;
    },
};

function createBaseIngressTLS(): IngressTLS {
    return { hosts: [], secretName: '' };
}

export const IngressTLS: MessageFns<IngressTLS> = {
    encode(message: IngressTLS, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.hosts) {
            writer.uint32(10).string(v!);
        }
        if (message.secretName !== undefined && message.secretName !== '') {
            writer.uint32(18).string(message.secretName);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): IngressTLS {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseIngressTLS();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.hosts.push(reader.string());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.secretName = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): IngressTLS {
        return {
            hosts: globalThis.Array.isArray(object?.hosts)
                ? object.hosts.map((e: any) => globalThis.String(e))
                : [],
            secretName: isSet(object.secretName) ? globalThis.String(object.secretName) : '',
        };
    },

    toJSON(message: IngressTLS): unknown {
        const obj: any = {};
        if (message.hosts?.length) {
            obj.hosts = message.hosts;
        }
        if (message.secretName !== undefined && message.secretName !== '') {
            obj.secretName = message.secretName;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<IngressTLS>, I>>(base?: I): IngressTLS {
        return IngressTLS.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<IngressTLS>, I>>(object: I): IngressTLS {
        const message = createBaseIngressTLS();
        message.hosts = object.hosts?.map((e) => e) || [];
        message.secretName = object.secretName ?? '';
        return message;
    },
};

function createBaseNetworkPolicy(): NetworkPolicy {
    return { metadata: undefined, spec: undefined };
}

export const NetworkPolicy: MessageFns<NetworkPolicy> = {
    encode(message: NetworkPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.spec !== undefined) {
            NetworkPolicySpec.encode(message.spec, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicy {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicy();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.spec = NetworkPolicySpec.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicy {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            spec: isSet(object.spec) ? NetworkPolicySpec.fromJSON(object.spec) : undefined,
        };
    },

    toJSON(message: NetworkPolicy): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.spec !== undefined) {
            obj.spec = NetworkPolicySpec.toJSON(message.spec);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicy>, I>>(base?: I): NetworkPolicy {
        return NetworkPolicy.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicy>, I>>(object: I): NetworkPolicy {
        const message = createBaseNetworkPolicy();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.spec =
            object.spec !== undefined && object.spec !== null
                ? NetworkPolicySpec.fromPartial(object.spec)
                : undefined;
        return message;
    },
};

function createBaseNetworkPolicyEgressRule(): NetworkPolicyEgressRule {
    return { ports: [], to: [] };
}

export const NetworkPolicyEgressRule: MessageFns<NetworkPolicyEgressRule> = {
    encode(message: NetworkPolicyEgressRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.ports) {
            NetworkPolicyPort.encode(v!, writer.uint32(10).fork()).join();
        }
        for (const v of message.to) {
            NetworkPolicyPeer.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicyEgressRule {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicyEgressRule();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.ports.push(NetworkPolicyPort.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.to.push(NetworkPolicyPeer.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicyEgressRule {
        return {
            ports: globalThis.Array.isArray(object?.ports)
                ? object.ports.map((e: any) => NetworkPolicyPort.fromJSON(e))
                : [],
            to: globalThis.Array.isArray(object?.to)
                ? object.to.map((e: any) => NetworkPolicyPeer.fromJSON(e))
                : [],
        };
    },

    toJSON(message: NetworkPolicyEgressRule): unknown {
        const obj: any = {};
        if (message.ports?.length) {
            obj.ports = message.ports.map((e) => NetworkPolicyPort.toJSON(e));
        }
        if (message.to?.length) {
            obj.to = message.to.map((e) => NetworkPolicyPeer.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicyEgressRule>, I>>(base?: I): NetworkPolicyEgressRule {
        return NetworkPolicyEgressRule.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicyEgressRule>, I>>(
        object: I,
    ): NetworkPolicyEgressRule {
        const message = createBaseNetworkPolicyEgressRule();
        message.ports = object.ports?.map((e) => NetworkPolicyPort.fromPartial(e)) || [];
        message.to = object.to?.map((e) => NetworkPolicyPeer.fromPartial(e)) || [];
        return message;
    },
};

function createBaseNetworkPolicyIngressRule(): NetworkPolicyIngressRule {
    return { ports: [], from: [] };
}

export const NetworkPolicyIngressRule: MessageFns<NetworkPolicyIngressRule> = {
    encode(message: NetworkPolicyIngressRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.ports) {
            NetworkPolicyPort.encode(v!, writer.uint32(10).fork()).join();
        }
        for (const v of message.from) {
            NetworkPolicyPeer.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicyIngressRule {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicyIngressRule();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.ports.push(NetworkPolicyPort.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.from.push(NetworkPolicyPeer.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicyIngressRule {
        return {
            ports: globalThis.Array.isArray(object?.ports)
                ? object.ports.map((e: any) => NetworkPolicyPort.fromJSON(e))
                : [],
            from: globalThis.Array.isArray(object?.from)
                ? object.from.map((e: any) => NetworkPolicyPeer.fromJSON(e))
                : [],
        };
    },

    toJSON(message: NetworkPolicyIngressRule): unknown {
        const obj: any = {};
        if (message.ports?.length) {
            obj.ports = message.ports.map((e) => NetworkPolicyPort.toJSON(e));
        }
        if (message.from?.length) {
            obj.from = message.from.map((e) => NetworkPolicyPeer.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicyIngressRule>, I>>(base?: I): NetworkPolicyIngressRule {
        return NetworkPolicyIngressRule.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicyIngressRule>, I>>(
        object: I,
    ): NetworkPolicyIngressRule {
        const message = createBaseNetworkPolicyIngressRule();
        message.ports = object.ports?.map((e) => NetworkPolicyPort.fromPartial(e)) || [];
        message.from = object.from?.map((e) => NetworkPolicyPeer.fromPartial(e)) || [];
        return message;
    },
};

function createBaseNetworkPolicyList(): NetworkPolicyList {
    return { metadata: undefined, items: [] };
}

export const NetworkPolicyList: MessageFns<NetworkPolicyList> = {
    encode(message: NetworkPolicyList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            NetworkPolicy.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicyList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicyList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(NetworkPolicy.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicyList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => NetworkPolicy.fromJSON(e))
                : [],
        };
    },

    toJSON(message: NetworkPolicyList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => NetworkPolicy.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicyList>, I>>(base?: I): NetworkPolicyList {
        return NetworkPolicyList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicyList>, I>>(object: I): NetworkPolicyList {
        const message = createBaseNetworkPolicyList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => NetworkPolicy.fromPartial(e)) || [];
        return message;
    },
};

function createBaseNetworkPolicyPeer(): NetworkPolicyPeer {
    return { podSelector: undefined, namespaceSelector: undefined, ipBlock: undefined };
}

export const NetworkPolicyPeer: MessageFns<NetworkPolicyPeer> = {
    encode(message: NetworkPolicyPeer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.podSelector !== undefined) {
            LabelSelector.encode(message.podSelector, writer.uint32(10).fork()).join();
        }
        if (message.namespaceSelector !== undefined) {
            LabelSelector.encode(message.namespaceSelector, writer.uint32(18).fork()).join();
        }
        if (message.ipBlock !== undefined) {
            IPBlock.encode(message.ipBlock, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicyPeer {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicyPeer();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.podSelector = LabelSelector.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.namespaceSelector = LabelSelector.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.ipBlock = IPBlock.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicyPeer {
        return {
            podSelector: isSet(object.podSelector) ? LabelSelector.fromJSON(object.podSelector) : undefined,
            namespaceSelector: isSet(object.namespaceSelector)
                ? LabelSelector.fromJSON(object.namespaceSelector)
                : undefined,
            ipBlock: isSet(object.ipBlock) ? IPBlock.fromJSON(object.ipBlock) : undefined,
        };
    },

    toJSON(message: NetworkPolicyPeer): unknown {
        const obj: any = {};
        if (message.podSelector !== undefined) {
            obj.podSelector = LabelSelector.toJSON(message.podSelector);
        }
        if (message.namespaceSelector !== undefined) {
            obj.namespaceSelector = LabelSelector.toJSON(message.namespaceSelector);
        }
        if (message.ipBlock !== undefined) {
            obj.ipBlock = IPBlock.toJSON(message.ipBlock);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicyPeer>, I>>(base?: I): NetworkPolicyPeer {
        return NetworkPolicyPeer.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicyPeer>, I>>(object: I): NetworkPolicyPeer {
        const message = createBaseNetworkPolicyPeer();
        message.podSelector =
            object.podSelector !== undefined && object.podSelector !== null
                ? LabelSelector.fromPartial(object.podSelector)
                : undefined;
        message.namespaceSelector =
            object.namespaceSelector !== undefined && object.namespaceSelector !== null
                ? LabelSelector.fromPartial(object.namespaceSelector)
                : undefined;
        message.ipBlock =
            object.ipBlock !== undefined && object.ipBlock !== null
                ? IPBlock.fromPartial(object.ipBlock)
                : undefined;
        return message;
    },
};

function createBaseNetworkPolicyPort(): NetworkPolicyPort {
    return { protocol: '', port: undefined, endPort: 0 };
}

export const NetworkPolicyPort: MessageFns<NetworkPolicyPort> = {
    encode(message: NetworkPolicyPort, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.protocol !== undefined && message.protocol !== '') {
            writer.uint32(10).string(message.protocol);
        }
        if (message.port !== undefined) {
            IntOrString.encode(message.port, writer.uint32(18).fork()).join();
        }
        if (message.endPort !== undefined && message.endPort !== 0) {
            writer.uint32(24).int32(message.endPort);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicyPort {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicyPort();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.protocol = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.port = IntOrString.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }

                        message.endPort = reader.int32();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicyPort {
        return {
            protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : '',
            port: isSet(object.port) ? IntOrString.fromJSON(object.port) : undefined,
            endPort: isSet(object.endPort) ? globalThis.Number(object.endPort) : 0,
        };
    },

    toJSON(message: NetworkPolicyPort): unknown {
        const obj: any = {};
        if (message.protocol !== undefined && message.protocol !== '') {
            obj.protocol = message.protocol;
        }
        if (message.port !== undefined) {
            obj.port = IntOrString.toJSON(message.port);
        }
        if (message.endPort !== undefined && message.endPort !== 0) {
            obj.endPort = Math.round(message.endPort);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicyPort>, I>>(base?: I): NetworkPolicyPort {
        return NetworkPolicyPort.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicyPort>, I>>(object: I): NetworkPolicyPort {
        const message = createBaseNetworkPolicyPort();
        message.protocol = object.protocol ?? '';
        message.port =
            object.port !== undefined && object.port !== null
                ? IntOrString.fromPartial(object.port)
                : undefined;
        message.endPort = object.endPort ?? 0;
        return message;
    },
};

function createBaseNetworkPolicySpec(): NetworkPolicySpec {
    return { podSelector: undefined, ingress: [], egress: [], policyTypes: [] };
}

export const NetworkPolicySpec: MessageFns<NetworkPolicySpec> = {
    encode(message: NetworkPolicySpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.podSelector !== undefined) {
            LabelSelector.encode(message.podSelector, writer.uint32(10).fork()).join();
        }
        for (const v of message.ingress) {
            NetworkPolicyIngressRule.encode(v!, writer.uint32(18).fork()).join();
        }
        for (const v of message.egress) {
            NetworkPolicyEgressRule.encode(v!, writer.uint32(26).fork()).join();
        }
        for (const v of message.policyTypes) {
            writer.uint32(34).string(v!);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): NetworkPolicySpec {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseNetworkPolicySpec();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.podSelector = LabelSelector.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.ingress.push(NetworkPolicyIngressRule.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.egress.push(NetworkPolicyEgressRule.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.policyTypes.push(reader.string());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): NetworkPolicySpec {
        return {
            podSelector: isSet(object.podSelector) ? LabelSelector.fromJSON(object.podSelector) : undefined,
            ingress: globalThis.Array.isArray(object?.ingress)
                ? object.ingress.map((e: any) => NetworkPolicyIngressRule.fromJSON(e))
                : [],
            egress: globalThis.Array.isArray(object?.egress)
                ? object.egress.map((e: any) => NetworkPolicyEgressRule.fromJSON(e))
                : [],
            policyTypes: globalThis.Array.isArray(object?.policyTypes)
                ? object.policyTypes.map((e: any) => globalThis.String(e))
                : [],
        };
    },

    toJSON(message: NetworkPolicySpec): unknown {
        const obj: any = {};
        if (message.podSelector !== undefined) {
            obj.podSelector = LabelSelector.toJSON(message.podSelector);
        }
        if (message.ingress?.length) {
            obj.ingress = message.ingress.map((e) => NetworkPolicyIngressRule.toJSON(e));
        }
        if (message.egress?.length) {
            obj.egress = message.egress.map((e) => NetworkPolicyEgressRule.toJSON(e));
        }
        if (message.policyTypes?.length) {
            obj.policyTypes = message.policyTypes;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<NetworkPolicySpec>, I>>(base?: I): NetworkPolicySpec {
        return NetworkPolicySpec.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<NetworkPolicySpec>, I>>(object: I): NetworkPolicySpec {
        const message = createBaseNetworkPolicySpec();
        message.podSelector =
            object.podSelector !== undefined && object.podSelector !== null
                ? LabelSelector.fromPartial(object.podSelector)
                : undefined;
        message.ingress = object.ingress?.map((e) => NetworkPolicyIngressRule.fromPartial(e)) || [];
        message.egress = object.egress?.map((e) => NetworkPolicyEgressRule.fromPartial(e)) || [];
        message.policyTypes = object.policyTypes?.map((e) => e) || [];
        return message;
    },
};

function createBaseReplicaSet(): ReplicaSet {
    return { metadata: undefined, spec: undefined, status: undefined };
}

export const ReplicaSet: MessageFns<ReplicaSet> = {
    encode(message: ReplicaSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.spec !== undefined) {
            ReplicaSetSpec.encode(message.spec, writer.uint32(18).fork()).join();
        }
        if (message.status !== undefined) {
            ReplicaSetStatus.encode(message.status, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ReplicaSet {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseReplicaSet();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.spec = ReplicaSetSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.status = ReplicaSetStatus.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ReplicaSet {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            spec: isSet(object.spec) ? ReplicaSetSpec.fromJSON(object.spec) : undefined,
            status: isSet(object.status) ? ReplicaSetStatus.fromJSON(object.status) : undefined,
        };
    },

    toJSON(message: ReplicaSet): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.spec !== undefined) {
            obj.spec = ReplicaSetSpec.toJSON(message.spec);
        }
        if (message.status !== undefined) {
            obj.status = ReplicaSetStatus.toJSON(message.status);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ReplicaSet>, I>>(base?: I): ReplicaSet {
        return ReplicaSet.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ReplicaSet>, I>>(object: I): ReplicaSet {
        const message = createBaseReplicaSet();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.spec =
            object.spec !== undefined && object.spec !== null
                ? ReplicaSetSpec.fromPartial(object.spec)
                : undefined;
        message.status =
            object.status !== undefined && object.status !== null
                ? ReplicaSetStatus.fromPartial(object.status)
                : undefined;
        return message;
    },
};

function createBaseReplicaSetCondition(): ReplicaSetCondition {
    return { type: '', status: '', lastTransitionTime: undefined, reason: '', message: '' };
}

export const ReplicaSetCondition: MessageFns<ReplicaSetCondition> = {
    encode(message: ReplicaSetCondition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.type !== undefined && message.type !== '') {
            writer.uint32(10).string(message.type);
        }
        if (message.status !== undefined && message.status !== '') {
            writer.uint32(18).string(message.status);
        }
        if (message.lastTransitionTime !== undefined) {
            Time.encode(message.lastTransitionTime, writer.uint32(26).fork()).join();
        }
        if (message.reason !== undefined && message.reason !== '') {
            writer.uint32(34).string(message.reason);
        }
        if (message.message !== undefined && message.message !== '') {
            writer.uint32(42).string(message.message);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ReplicaSetCondition {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseReplicaSetCondition();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.type = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.status = reader.string();
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.lastTransitionTime = Time.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.reason = reader.string();
                        continue;
                    }
                    case 5: {
                        if (tag !== 42) {
                            break;
                        }

                        message.message = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ReplicaSetCondition {
        return {
            type: isSet(object.type) ? globalThis.String(object.type) : '',
            status: isSet(object.status) ? globalThis.String(object.status) : '',
            lastTransitionTime: isSet(object.lastTransitionTime)
                ? Time.fromJSON(object.lastTransitionTime)
                : undefined,
            reason: isSet(object.reason) ? globalThis.String(object.reason) : '',
            message: isSet(object.message) ? globalThis.String(object.message) : '',
        };
    },

    toJSON(message: ReplicaSetCondition): unknown {
        const obj: any = {};
        if (message.type !== undefined && message.type !== '') {
            obj.type = message.type;
        }
        if (message.status !== undefined && message.status !== '') {
            obj.status = message.status;
        }
        if (message.lastTransitionTime !== undefined) {
            obj.lastTransitionTime = Time.toJSON(message.lastTransitionTime);
        }
        if (message.reason !== undefined && message.reason !== '') {
            obj.reason = message.reason;
        }
        if (message.message !== undefined && message.message !== '') {
            obj.message = message.message;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ReplicaSetCondition>, I>>(base?: I): ReplicaSetCondition {
        return ReplicaSetCondition.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ReplicaSetCondition>, I>>(object: I): ReplicaSetCondition {
        const message = createBaseReplicaSetCondition();
        message.type = object.type ?? '';
        message.status = object.status ?? '';
        message.lastTransitionTime =
            object.lastTransitionTime !== undefined && object.lastTransitionTime !== null
                ? Time.fromPartial(object.lastTransitionTime)
                : undefined;
        message.reason = object.reason ?? '';
        message.message = object.message ?? '';
        return message;
    },
};

function createBaseReplicaSetList(): ReplicaSetList {
    return { metadata: undefined, items: [] };
}

export const ReplicaSetList: MessageFns<ReplicaSetList> = {
    encode(message: ReplicaSetList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            ReplicaSet.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ReplicaSetList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseReplicaSetList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(ReplicaSet.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ReplicaSetList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => ReplicaSet.fromJSON(e))
                : [],
        };
    },

    toJSON(message: ReplicaSetList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => ReplicaSet.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ReplicaSetList>, I>>(base?: I): ReplicaSetList {
        return ReplicaSetList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ReplicaSetList>, I>>(object: I): ReplicaSetList {
        const message = createBaseReplicaSetList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => ReplicaSet.fromPartial(e)) || [];
        return message;
    },
};

function createBaseReplicaSetSpec(): ReplicaSetSpec {
    return { replicas: 0, minReadySeconds: 0, selector: undefined, template: undefined };
}

export const ReplicaSetSpec: MessageFns<ReplicaSetSpec> = {
    encode(message: ReplicaSetSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.replicas !== undefined && message.replicas !== 0) {
            writer.uint32(8).int32(message.replicas);
        }
        if (message.minReadySeconds !== undefined && message.minReadySeconds !== 0) {
            writer.uint32(32).int32(message.minReadySeconds);
        }
        if (message.selector !== undefined) {
            LabelSelector.encode(message.selector, writer.uint32(18).fork()).join();
        }
        if (message.template !== undefined) {
            PodTemplateSpec.encode(message.template, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ReplicaSetSpec {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseReplicaSetSpec();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.replicas = reader.int32();
                        continue;
                    }
                    case 4: {
                        if (tag !== 32) {
                            break;
                        }

                        message.minReadySeconds = reader.int32();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.selector = LabelSelector.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.template = PodTemplateSpec.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ReplicaSetSpec {
        return {
            replicas: isSet(object.replicas) ? globalThis.Number(object.replicas) : 0,
            minReadySeconds: isSet(object.minReadySeconds) ? globalThis.Number(object.minReadySeconds) : 0,
            selector: isSet(object.selector) ? LabelSelector.fromJSON(object.selector) : undefined,
            template: isSet(object.template) ? PodTemplateSpec.fromJSON(object.template) : undefined,
        };
    },

    toJSON(message: ReplicaSetSpec): unknown {
        const obj: any = {};
        if (message.replicas !== undefined && message.replicas !== 0) {
            obj.replicas = Math.round(message.replicas);
        }
        if (message.minReadySeconds !== undefined && message.minReadySeconds !== 0) {
            obj.minReadySeconds = Math.round(message.minReadySeconds);
        }
        if (message.selector !== undefined) {
            obj.selector = LabelSelector.toJSON(message.selector);
        }
        if (message.template !== undefined) {
            obj.template = PodTemplateSpec.toJSON(message.template);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ReplicaSetSpec>, I>>(base?: I): ReplicaSetSpec {
        return ReplicaSetSpec.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ReplicaSetSpec>, I>>(object: I): ReplicaSetSpec {
        const message = createBaseReplicaSetSpec();
        message.replicas = object.replicas ?? 0;
        message.minReadySeconds = object.minReadySeconds ?? 0;
        message.selector =
            object.selector !== undefined && object.selector !== null
                ? LabelSelector.fromPartial(object.selector)
                : undefined;
        message.template =
            object.template !== undefined && object.template !== null
                ? PodTemplateSpec.fromPartial(object.template)
                : undefined;
        return message;
    },
};

function createBaseReplicaSetStatus(): ReplicaSetStatus {
    return {
        replicas: 0,
        fullyLabeledReplicas: 0,
        readyReplicas: 0,
        availableReplicas: 0,
        terminatingReplicas: 0,
        observedGeneration: 0,
        conditions: [],
    };
}

export const ReplicaSetStatus: MessageFns<ReplicaSetStatus> = {
    encode(message: ReplicaSetStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.replicas !== undefined && message.replicas !== 0) {
            writer.uint32(8).int32(message.replicas);
        }
        if (message.fullyLabeledReplicas !== undefined && message.fullyLabeledReplicas !== 0) {
            writer.uint32(16).int32(message.fullyLabeledReplicas);
        }
        if (message.readyReplicas !== undefined && message.readyReplicas !== 0) {
            writer.uint32(32).int32(message.readyReplicas);
        }
        if (message.availableReplicas !== undefined && message.availableReplicas !== 0) {
            writer.uint32(40).int32(message.availableReplicas);
        }
        if (message.terminatingReplicas !== undefined && message.terminatingReplicas !== 0) {
            writer.uint32(56).int32(message.terminatingReplicas);
        }
        if (message.observedGeneration !== undefined && message.observedGeneration !== 0) {
            writer.uint32(24).int64(message.observedGeneration);
        }
        for (const v of message.conditions) {
            ReplicaSetCondition.encode(v!, writer.uint32(50).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ReplicaSetStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseReplicaSetStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.replicas = reader.int32();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }

                        message.fullyLabeledReplicas = reader.int32();
                        continue;
                    }
                    case 4: {
                        if (tag !== 32) {
                            break;
                        }

                        message.readyReplicas = reader.int32();
                        continue;
                    }
                    case 5: {
                        if (tag !== 40) {
                            break;
                        }

                        message.availableReplicas = reader.int32();
                        continue;
                    }
                    case 7: {
                        if (tag !== 56) {
                            break;
                        }

                        message.terminatingReplicas = reader.int32();
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }

                        message.observedGeneration = longToNumber(reader.int64());
                        continue;
                    }
                    case 6: {
                        if (tag !== 50) {
                            break;
                        }

                        message.conditions.push(ReplicaSetCondition.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ReplicaSetStatus {
        return {
            replicas: isSet(object.replicas) ? globalThis.Number(object.replicas) : 0,
            fullyLabeledReplicas: isSet(object.fullyLabeledReplicas)
                ? globalThis.Number(object.fullyLabeledReplicas)
                : 0,
            readyReplicas: isSet(object.readyReplicas) ? globalThis.Number(object.readyReplicas) : 0,
            availableReplicas: isSet(object.availableReplicas)
                ? globalThis.Number(object.availableReplicas)
                : 0,
            terminatingReplicas: isSet(object.terminatingReplicas)
                ? globalThis.Number(object.terminatingReplicas)
                : 0,
            observedGeneration: isSet(object.observedGeneration)
                ? globalThis.Number(object.observedGeneration)
                : 0,
            conditions: globalThis.Array.isArray(object?.conditions)
                ? object.conditions.map((e: any) => ReplicaSetCondition.fromJSON(e))
                : [],
        };
    },

    toJSON(message: ReplicaSetStatus): unknown {
        const obj: any = {};
        if (message.replicas !== undefined && message.replicas !== 0) {
            obj.replicas = Math.round(message.replicas);
        }
        if (message.fullyLabeledReplicas !== undefined && message.fullyLabeledReplicas !== 0) {
            obj.fullyLabeledReplicas = Math.round(message.fullyLabeledReplicas);
        }
        if (message.readyReplicas !== undefined && message.readyReplicas !== 0) {
            obj.readyReplicas = Math.round(message.readyReplicas);
        }
        if (message.availableReplicas !== undefined && message.availableReplicas !== 0) {
            obj.availableReplicas = Math.round(message.availableReplicas);
        }
        if (message.terminatingReplicas !== undefined && message.terminatingReplicas !== 0) {
            obj.terminatingReplicas = Math.round(message.terminatingReplicas);
        }
        if (message.observedGeneration !== undefined && message.observedGeneration !== 0) {
            obj.observedGeneration = Math.round(message.observedGeneration);
        }
        if (message.conditions?.length) {
            obj.conditions = message.conditions.map((e) => ReplicaSetCondition.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ReplicaSetStatus>, I>>(base?: I): ReplicaSetStatus {
        return ReplicaSetStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ReplicaSetStatus>, I>>(object: I): ReplicaSetStatus {
        const message = createBaseReplicaSetStatus();
        message.replicas = object.replicas ?? 0;
        message.fullyLabeledReplicas = object.fullyLabeledReplicas ?? 0;
        message.readyReplicas = object.readyReplicas ?? 0;
        message.availableReplicas = object.availableReplicas ?? 0;
        message.terminatingReplicas = object.terminatingReplicas ?? 0;
        message.observedGeneration = object.observedGeneration ?? 0;
        message.conditions = object.conditions?.map((e) => ReplicaSetCondition.fromPartial(e)) || [];
        return message;
    },
};

function createBaseRollbackConfig(): RollbackConfig {
    return { revision: 0 };
}

export const RollbackConfig: MessageFns<RollbackConfig> = {
    encode(message: RollbackConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.revision !== undefined && message.revision !== 0) {
            writer.uint32(8).int64(message.revision);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): RollbackConfig {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseRollbackConfig();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.revision = longToNumber(reader.int64());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): RollbackConfig {
        return { revision: isSet(object.revision) ? globalThis.Number(object.revision) : 0 };
    },

    toJSON(message: RollbackConfig): unknown {
        const obj: any = {};
        if (message.revision !== undefined && message.revision !== 0) {
            obj.revision = Math.round(message.revision);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<RollbackConfig>, I>>(base?: I): RollbackConfig {
        return RollbackConfig.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<RollbackConfig>, I>>(object: I): RollbackConfig {
        const message = createBaseRollbackConfig();
        message.revision = object.revision ?? 0;
        return message;
    },
};

function createBaseRollingUpdateDaemonSet(): RollingUpdateDaemonSet {
    return { maxUnavailable: undefined, maxSurge: undefined };
}

export const RollingUpdateDaemonSet: MessageFns<RollingUpdateDaemonSet> = {
    encode(message: RollingUpdateDaemonSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.maxUnavailable !== undefined) {
            IntOrString.encode(message.maxUnavailable, writer.uint32(10).fork()).join();
        }
        if (message.maxSurge !== undefined) {
            IntOrString.encode(message.maxSurge, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): RollingUpdateDaemonSet {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseRollingUpdateDaemonSet();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.maxUnavailable = IntOrString.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.maxSurge = IntOrString.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): RollingUpdateDaemonSet {
        return {
            maxUnavailable: isSet(object.maxUnavailable)
                ? IntOrString.fromJSON(object.maxUnavailable)
                : undefined,
            maxSurge: isSet(object.maxSurge) ? IntOrString.fromJSON(object.maxSurge) : undefined,
        };
    },

    toJSON(message: RollingUpdateDaemonSet): unknown {
        const obj: any = {};
        if (message.maxUnavailable !== undefined) {
            obj.maxUnavailable = IntOrString.toJSON(message.maxUnavailable);
        }
        if (message.maxSurge !== undefined) {
            obj.maxSurge = IntOrString.toJSON(message.maxSurge);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<RollingUpdateDaemonSet>, I>>(base?: I): RollingUpdateDaemonSet {
        return RollingUpdateDaemonSet.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<RollingUpdateDaemonSet>, I>>(object: I): RollingUpdateDaemonSet {
        const message = createBaseRollingUpdateDaemonSet();
        message.maxUnavailable =
            object.maxUnavailable !== undefined && object.maxUnavailable !== null
                ? IntOrString.fromPartial(object.maxUnavailable)
                : undefined;
        message.maxSurge =
            object.maxSurge !== undefined && object.maxSurge !== null
                ? IntOrString.fromPartial(object.maxSurge)
                : undefined;
        return message;
    },
};

function createBaseRollingUpdateDeployment(): RollingUpdateDeployment {
    return { maxUnavailable: undefined, maxSurge: undefined };
}

export const RollingUpdateDeployment: MessageFns<RollingUpdateDeployment> = {
    encode(message: RollingUpdateDeployment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.maxUnavailable !== undefined) {
            IntOrString.encode(message.maxUnavailable, writer.uint32(10).fork()).join();
        }
        if (message.maxSurge !== undefined) {
            IntOrString.encode(message.maxSurge, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): RollingUpdateDeployment {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseRollingUpdateDeployment();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.maxUnavailable = IntOrString.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.maxSurge = IntOrString.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): RollingUpdateDeployment {
        return {
            maxUnavailable: isSet(object.maxUnavailable)
                ? IntOrString.fromJSON(object.maxUnavailable)
                : undefined,
            maxSurge: isSet(object.maxSurge) ? IntOrString.fromJSON(object.maxSurge) : undefined,
        };
    },

    toJSON(message: RollingUpdateDeployment): unknown {
        const obj: any = {};
        if (message.maxUnavailable !== undefined) {
            obj.maxUnavailable = IntOrString.toJSON(message.maxUnavailable);
        }
        if (message.maxSurge !== undefined) {
            obj.maxSurge = IntOrString.toJSON(message.maxSurge);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<RollingUpdateDeployment>, I>>(base?: I): RollingUpdateDeployment {
        return RollingUpdateDeployment.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<RollingUpdateDeployment>, I>>(
        object: I,
    ): RollingUpdateDeployment {
        const message = createBaseRollingUpdateDeployment();
        message.maxUnavailable =
            object.maxUnavailable !== undefined && object.maxUnavailable !== null
                ? IntOrString.fromPartial(object.maxUnavailable)
                : undefined;
        message.maxSurge =
            object.maxSurge !== undefined && object.maxSurge !== null
                ? IntOrString.fromPartial(object.maxSurge)
                : undefined;
        return message;
    },
};

function createBaseScale(): Scale {
    return { metadata: undefined, spec: undefined, status: undefined };
}

export const Scale: MessageFns<Scale> = {
    encode(message: Scale, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.spec !== undefined) {
            ScaleSpec.encode(message.spec, writer.uint32(18).fork()).join();
        }
        if (message.status !== undefined) {
            ScaleStatus.encode(message.status, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Scale {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseScale();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.spec = ScaleSpec.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.status = ScaleStatus.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Scale {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            spec: isSet(object.spec) ? ScaleSpec.fromJSON(object.spec) : undefined,
            status: isSet(object.status) ? ScaleStatus.fromJSON(object.status) : undefined,
        };
    },

    toJSON(message: Scale): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.spec !== undefined) {
            obj.spec = ScaleSpec.toJSON(message.spec);
        }
        if (message.status !== undefined) {
            obj.status = ScaleStatus.toJSON(message.status);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Scale>, I>>(base?: I): Scale {
        return Scale.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Scale>, I>>(object: I): Scale {
        const message = createBaseScale();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.spec =
            object.spec !== undefined && object.spec !== null
                ? ScaleSpec.fromPartial(object.spec)
                : undefined;
        message.status =
            object.status !== undefined && object.status !== null
                ? ScaleStatus.fromPartial(object.status)
                : undefined;
        return message;
    },
};

function createBaseScaleSpec(): ScaleSpec {
    return { replicas: 0 };
}

export const ScaleSpec: MessageFns<ScaleSpec> = {
    encode(message: ScaleSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.replicas !== undefined && message.replicas !== 0) {
            writer.uint32(8).int32(message.replicas);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ScaleSpec {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseScaleSpec();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.replicas = reader.int32();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ScaleSpec {
        return { replicas: isSet(object.replicas) ? globalThis.Number(object.replicas) : 0 };
    },

    toJSON(message: ScaleSpec): unknown {
        const obj: any = {};
        if (message.replicas !== undefined && message.replicas !== 0) {
            obj.replicas = Math.round(message.replicas);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ScaleSpec>, I>>(base?: I): ScaleSpec {
        return ScaleSpec.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ScaleSpec>, I>>(object: I): ScaleSpec {
        const message = createBaseScaleSpec();
        message.replicas = object.replicas ?? 0;
        return message;
    },
};

function createBaseScaleStatus(): ScaleStatus {
    return { replicas: 0, selector: {}, targetSelector: '' };
}

export const ScaleStatus: MessageFns<ScaleStatus> = {
    encode(message: ScaleStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.replicas !== undefined && message.replicas !== 0) {
            writer.uint32(8).int32(message.replicas);
        }
        globalThis.Object.entries(message.selector).forEach(([key, value]: [string, string]) => {
            ScaleStatus_SelectorEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
        });
        if (message.targetSelector !== undefined && message.targetSelector !== '') {
            writer.uint32(26).string(message.targetSelector);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ScaleStatus {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseScaleStatus();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.replicas = reader.int32();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        const entry2 = ScaleStatus_SelectorEntry.decode(reader, reader.uint32());
                        if (entry2.value !== undefined) {
                            message.selector[entry2.key] = entry2.value;
                        }
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.targetSelector = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ScaleStatus {
        return {
            replicas: isSet(object.replicas) ? globalThis.Number(object.replicas) : 0,
            selector: isObject(object.selector)
                ? (globalThis.Object.entries(object.selector) as [string, any][]).reduce(
                      (acc: { [key: string]: string }, [key, value]: [string, any]) => {
                          globalThis.Object.defineProperty(acc, key, {
                              value: globalThis.String(value),
                              enumerable: true,
                              configurable: true,
                              writable: true,
                          });
                          return acc;
                      },
                      {},
                  )
                : {},
            targetSelector: isSet(object.targetSelector) ? globalThis.String(object.targetSelector) : '',
        };
    },

    toJSON(message: ScaleStatus): unknown {
        const obj: any = {};
        if (message.replicas !== undefined && message.replicas !== 0) {
            obj.replicas = Math.round(message.replicas);
        }
        if (message.selector) {
            const entries = globalThis.Object.entries(message.selector) as [string, string][];
            if (entries.length > 0) {
                obj.selector = {};
                entries.forEach(([k, v]) => {
                    obj.selector[k] = v;
                });
            }
        }
        if (message.targetSelector !== undefined && message.targetSelector !== '') {
            obj.targetSelector = message.targetSelector;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ScaleStatus>, I>>(base?: I): ScaleStatus {
        return ScaleStatus.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ScaleStatus>, I>>(object: I): ScaleStatus {
        const message = createBaseScaleStatus();
        message.replicas = object.replicas ?? 0;
        message.selector = (globalThis.Object.entries(object.selector ?? {}) as [string, string][]).reduce(
            (acc: { [key: string]: string }, [key, value]: [string, string]) => {
                if (value !== undefined) {
                    acc[key] = globalThis.String(value);
                }
                return acc;
            },
            {},
        );
        message.targetSelector = object.targetSelector ?? '';
        return message;
    },
};

function createBaseScaleStatus_SelectorEntry(): ScaleStatus_SelectorEntry {
    return { key: '', value: '' };
}

export const ScaleStatus_SelectorEntry: MessageFns<ScaleStatus_SelectorEntry> = {
    encode(message: ScaleStatus_SelectorEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.key !== '') {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== '') {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ScaleStatus_SelectorEntry {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseScaleStatus_SelectorEntry();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.key = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.value = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ScaleStatus_SelectorEntry {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : '',
            value: isSet(object.value) ? globalThis.String(object.value) : '',
        };
    },

    toJSON(message: ScaleStatus_SelectorEntry): unknown {
        const obj: any = {};
        if (message.key !== '') {
            obj.key = message.key;
        }
        if (message.value !== '') {
            obj.value = message.value;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ScaleStatus_SelectorEntry>, I>>(base?: I): ScaleStatus_SelectorEntry {
        return ScaleStatus_SelectorEntry.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ScaleStatus_SelectorEntry>, I>>(
        object: I,
    ): ScaleStatus_SelectorEntry {
        const message = createBaseScaleStatus_SelectorEntry();
        message.key = object.key ?? '';
        message.value = object.value ?? '';
        return message;
    },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
    ? T
    : T extends globalThis.Array<infer U>
      ? globalThis.Array<DeepPartial<U>>
      : T extends ReadonlyArray<infer U>
        ? ReadonlyArray<DeepPartial<U>>
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
    ? P
    : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER');
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER');
    }
    return num;
}

function isObject(value: any): boolean {
    return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
    return value !== null && value !== undefined;
}

interface MessageFns<T> {
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
    fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
