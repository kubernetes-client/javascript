// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v3.6.1
// source: k8s.io/api/discovery/v1/generated.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { ListMeta, ObjectMeta } from '../../../apimachinery/pkg/apis/meta/v1/generated.js';
import { ObjectReference } from '../../core/v1/generated.js';

/** Endpoint represents a single logical "backend" implementing a service. */
export interface Endpoint {
    /**
     * addresses of this endpoint. For EndpointSlices of addressType "IPv4" or "IPv6",
     * the values are IP addresses in canonical form. The syntax and semantics of
     * other addressType values are not defined. This must contain at least one
     * address but no more than 100. EndpointSlices generated by the EndpointSlice
     * controller will always have exactly 1 address. No semantics are defined for
     * additional addresses beyond the first, and kube-proxy does not look at them.
     * +listType=set
     * +required
     * +k8s:beta(since: "1.37")=+k8s:required
     * +k8s:beta(since: "1.37")=+k8s:maxItems=100
     */
    addresses: string[];
    /**
     * conditions contains information about the current status of the endpoint.
     * +optional
     */
    conditions?: EndpointConditions | undefined;
    /**
     * hostname of this endpoint. This field may be used by consumers of
     * endpoints to distinguish endpoints from each other (e.g. in DNS names).
     * Multiple endpoints which use the same hostname should be considered
     * fungible (e.g. multiple A values in DNS). Must be lowercase and pass DNS
     * Label (RFC 1123) validation.
     * +optional
     */
    hostname?: string | undefined;
    /**
     * targetRef is a reference to a Kubernetes object that represents this
     * endpoint.
     * +optional
     */
    targetRef?: ObjectReference | undefined;
    /**
     * deprecatedTopology contains topology information part of the v1beta1
     * API. This field is deprecated, and will be removed when the v1beta1
     * API is removed (no sooner than kubernetes v1.24).  While this field can
     * hold values, it is not writable through the v1 API, and any attempts to
     * write to it will be silently ignored. Topology information can be found
     * in the zone and nodeName fields instead.
     * +optional
     */
    deprecatedTopology: { [key: string]: string };
    /**
     * nodeName represents the name of the Node hosting this endpoint. This can
     * be used to determine endpoints local to a Node.
     * +optional
     */
    nodeName?: string | undefined;
    /**
     * zone is the name of the Zone this endpoint exists in.
     * +optional
     */
    zone?: string | undefined;
    /**
     * hints contains information associated with how an endpoint should be
     * consumed.
     * +optional
     */
    hints?: EndpointHints | undefined;
}

export interface Endpoint_DeprecatedTopologyEntry {
    key: string;
    value: string;
}

/** EndpointConditions represents the current condition of an endpoint. */
export interface EndpointConditions {
    /**
     * ready indicates that this endpoint is ready to receive traffic,
     * according to whatever system is managing the endpoint. A nil value
     * should be interpreted as "true". In general, an endpoint should be
     * marked ready if it is serving and not terminating, though this can
     * be overridden in some cases, such as when the associated Service has
     * set the publishNotReadyAddresses flag.
     * +optional
     */
    ready?: boolean | undefined;
    /**
     * serving indicates that this endpoint is able to receive traffic,
     * according to whatever system is managing the endpoint. For endpoints
     * backed by pods, the EndpointSlice controller will mark the endpoint
     * as serving if the pod's Ready condition is True. A nil value should be
     * interpreted as "true".
     * +optional
     */
    serving?: boolean | undefined;
    /**
     * terminating indicates that this endpoint is terminating. A nil value
     * should be interpreted as "false".
     * +optional
     */
    terminating?: boolean | undefined;
}

/** EndpointHints provides hints describing how an endpoint should be consumed. */
export interface EndpointHints {
    /**
     * forZones indicates the zone(s) this endpoint should be consumed by when
     * using topology aware routing. May contain a maximum of 8 entries.
     * +optional
     * +listType=atomic
     */
    forZones: ForZone[];
    /**
     * forNodes indicates the node(s) this endpoint should be consumed by when
     * using topology aware routing. May contain a maximum of 8 entries.
     * +optional
     * +listType=atomic
     */
    forNodes: ForNode[];
}

/**
 * EndpointPort represents a Port used by an EndpointSlice
 * +structType=atomic
 */
export interface EndpointPort {
    /**
     * name represents the name of this port. All ports in an EndpointSlice must have a unique name.
     * If the EndpointSlice is derived from a Kubernetes service, this corresponds to the Service.ports[].name.
     * Name must either be an empty string or pass DNS_LABEL validation:
     * * must be no more than 63 characters long.
     * * must consist of lower case alphanumeric characters or '-'.
     * * must start and end with an alphanumeric character.
     * Default is empty string.
     * +optional
     */
    name?: string | undefined;
    /**
     * protocol represents the IP protocol for this port.
     * Must be UDP, TCP, or SCTP.
     * Default is TCP.
     * +optional
     */
    protocol?: string | undefined;
    /**
     * port represents the port number of the endpoint.
     * If the EndpointSlice is derived from a Kubernetes service, this must be set
     * to the service's target port. EndpointSlices used for other purposes may have
     * a nil port.
     * +optional
     */
    port?: number | undefined;
    /**
     * The application protocol for this port.
     * This is used as a hint for implementations to offer richer behavior for protocols that they understand.
     * This field follows standard Kubernetes label syntax.
     * Valid values are either:
     *
     * * Un-prefixed protocol names - reserved for IANA standard service names (as per
     * RFC-6335 and https://www.iana.org/assignments/service-names).
     *
     * * Kubernetes-defined prefixed names:
     *   * 'kubernetes.io/h2c' - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-
     *   * 'kubernetes.io/ws'  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455
     *   * 'kubernetes.io/wss' - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455
     *
     * * Other protocols should use implementation-defined prefixed names such as
     * mycompany.com/my-custom-protocol.
     * +optional
     */
    appProtocol?: string | undefined;
}

/**
 * EndpointSlice represents a set of service endpoints. Most EndpointSlices are created by
 * the EndpointSlice controller to represent the Pods selected by Service objects. For a
 * given service there may be multiple EndpointSlice objects which must be joined to
 * produce the full set of endpoints; you can find all of the slices for a given service
 * by listing EndpointSlices in the service's namespace whose `kubernetes.io/service-name`
 * label contains the service's name.
 */
export interface EndpointSlice {
    /**
     * Standard object's metadata.
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * addressType specifies the type of address carried by this EndpointSlice.
     * All addresses in this slice must be the same type. This field is
     * immutable after creation. The following address types are currently
     * supported:
     * * IPv4: Represents an IPv4 Address.
     * * IPv6: Represents an IPv6 Address.
     * * FQDN: Represents a Fully Qualified Domain Name. (Deprecated)
     * The EndpointSlice controller only generates, and kube-proxy only processes,
     * slices of addressType "IPv4" and "IPv6". No semantics are defined for
     * the "FQDN" type.
     * +required
     * +k8s:beta(since: "1.37")=+k8s:required
     * +k8s:beta(since: "1.37")=+k8s:immutable
     */
    addressType?: string | undefined;
    /**
     * endpoints is a list of unique endpoints in this slice. Each slice may
     * include a maximum of 1000 endpoints.
     * +optional
     * +listType=atomic
     * +k8s:beta(since: "1.37")=+k8s:optional
     */
    endpoints: Endpoint[];
    /**
     * ports specifies the list of network ports exposed by each endpoint in
     * this slice. Each port must have a unique name. Each slice may include a
     * maximum of 100 ports.
     * Services always have at least 1 port, so EndpointSlices generated by the
     * EndpointSlice controller will likewise always have at least 1 port.
     * EndpointSlices used for other purposes may have an empty ports list.
     * +optional
     * +listType=atomic
     */
    ports: EndpointPort[];
}

/** EndpointSliceList represents a list of endpoint slices */
export interface EndpointSliceList {
    /**
     * Standard list metadata.
     * +optional
     */
    metadata?: ListMeta | undefined;
    /** items is the list of endpoint slices */
    items: EndpointSlice[];
}

/** ForNode provides information about which nodes should consume this endpoint. */
export interface ForNode {
    /**
     * name represents the name of the node.
     * +required
     */
    name?: string | undefined;
}

/** ForZone provides information about which zones should consume this endpoint. */
export interface ForZone {
    /**
     * name represents the name of the zone.
     * +required
     */
    name?: string | undefined;
}

function createBaseEndpoint(): Endpoint {
    return {
        addresses: [],
        conditions: undefined,
        hostname: '',
        targetRef: undefined,
        deprecatedTopology: {},
        nodeName: '',
        zone: '',
        hints: undefined,
    };
}

export const Endpoint: MessageFns<Endpoint> = {
    encode(message: Endpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.addresses) {
            writer.uint32(10).string(v!);
        }
        if (message.conditions !== undefined) {
            EndpointConditions.encode(message.conditions, writer.uint32(18).fork()).join();
        }
        if (message.hostname !== undefined && message.hostname !== '') {
            writer.uint32(26).string(message.hostname);
        }
        if (message.targetRef !== undefined) {
            ObjectReference.encode(message.targetRef, writer.uint32(34).fork()).join();
        }
        globalThis.Object.entries(message.deprecatedTopology).forEach(([key, value]: [string, string]) => {
            Endpoint_DeprecatedTopologyEntry.encode(
                { key: key as any, value },
                writer.uint32(42).fork(),
            ).join();
        });
        if (message.nodeName !== undefined && message.nodeName !== '') {
            writer.uint32(50).string(message.nodeName);
        }
        if (message.zone !== undefined && message.zone !== '') {
            writer.uint32(58).string(message.zone);
        }
        if (message.hints !== undefined) {
            EndpointHints.encode(message.hints, writer.uint32(66).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Endpoint {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpoint();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.addresses.push(reader.string());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.conditions = EndpointConditions.decode(reader, reader.uint32());
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.hostname = reader.string();
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.targetRef = ObjectReference.decode(reader, reader.uint32());
                        continue;
                    }
                    case 5: {
                        if (tag !== 42) {
                            break;
                        }

                        const entry5 = Endpoint_DeprecatedTopologyEntry.decode(reader, reader.uint32());
                        if (entry5.value !== undefined) {
                            message.deprecatedTopology[entry5.key] = entry5.value;
                        }
                        continue;
                    }
                    case 6: {
                        if (tag !== 50) {
                            break;
                        }

                        message.nodeName = reader.string();
                        continue;
                    }
                    case 7: {
                        if (tag !== 58) {
                            break;
                        }

                        message.zone = reader.string();
                        continue;
                    }
                    case 8: {
                        if (tag !== 66) {
                            break;
                        }

                        message.hints = EndpointHints.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Endpoint {
        return {
            addresses: globalThis.Array.isArray(object?.addresses)
                ? object.addresses.map((e: any) => globalThis.String(e))
                : [],
            conditions: isSet(object.conditions) ? EndpointConditions.fromJSON(object.conditions) : undefined,
            hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : '',
            targetRef: isSet(object.targetRef) ? ObjectReference.fromJSON(object.targetRef) : undefined,
            deprecatedTopology: isObject(object.deprecatedTopology)
                ? (globalThis.Object.entries(object.deprecatedTopology) as [string, any][]).reduce(
                      (acc: { [key: string]: string }, [key, value]: [string, any]) => {
                          globalThis.Object.defineProperty(acc, key, {
                              value: globalThis.String(value),
                              enumerable: true,
                              configurable: true,
                              writable: true,
                          });
                          return acc;
                      },
                      {},
                  )
                : {},
            nodeName: isSet(object.nodeName) ? globalThis.String(object.nodeName) : '',
            zone: isSet(object.zone) ? globalThis.String(object.zone) : '',
            hints: isSet(object.hints) ? EndpointHints.fromJSON(object.hints) : undefined,
        };
    },

    toJSON(message: Endpoint): unknown {
        const obj: any = {};
        if (message.addresses?.length) {
            obj.addresses = message.addresses;
        }
        if (message.conditions !== undefined) {
            obj.conditions = EndpointConditions.toJSON(message.conditions);
        }
        if (message.hostname !== undefined && message.hostname !== '') {
            obj.hostname = message.hostname;
        }
        if (message.targetRef !== undefined) {
            obj.targetRef = ObjectReference.toJSON(message.targetRef);
        }
        if (message.deprecatedTopology) {
            const entries = globalThis.Object.entries(message.deprecatedTopology) as [string, string][];
            if (entries.length > 0) {
                obj.deprecatedTopology = {};
                entries.forEach(([k, v]) => {
                    obj.deprecatedTopology[k] = v;
                });
            }
        }
        if (message.nodeName !== undefined && message.nodeName !== '') {
            obj.nodeName = message.nodeName;
        }
        if (message.zone !== undefined && message.zone !== '') {
            obj.zone = message.zone;
        }
        if (message.hints !== undefined) {
            obj.hints = EndpointHints.toJSON(message.hints);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Endpoint>, I>>(base?: I): Endpoint {
        return Endpoint.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Endpoint>, I>>(object: I): Endpoint {
        const message = createBaseEndpoint();
        message.addresses = object.addresses?.map((e) => e) || [];
        message.conditions =
            object.conditions !== undefined && object.conditions !== null
                ? EndpointConditions.fromPartial(object.conditions)
                : undefined;
        message.hostname = object.hostname ?? '';
        message.targetRef =
            object.targetRef !== undefined && object.targetRef !== null
                ? ObjectReference.fromPartial(object.targetRef)
                : undefined;
        message.deprecatedTopology = (
            globalThis.Object.entries(object.deprecatedTopology ?? {}) as [string, string][]
        ).reduce((acc: { [key: string]: string }, [key, value]: [string, string]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        message.nodeName = object.nodeName ?? '';
        message.zone = object.zone ?? '';
        message.hints =
            object.hints !== undefined && object.hints !== null
                ? EndpointHints.fromPartial(object.hints)
                : undefined;
        return message;
    },
};

function createBaseEndpoint_DeprecatedTopologyEntry(): Endpoint_DeprecatedTopologyEntry {
    return { key: '', value: '' };
}

export const Endpoint_DeprecatedTopologyEntry: MessageFns<Endpoint_DeprecatedTopologyEntry> = {
    encode(
        message: Endpoint_DeprecatedTopologyEntry,
        writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
        if (message.key !== '') {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== '') {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Endpoint_DeprecatedTopologyEntry {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpoint_DeprecatedTopologyEntry();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.key = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.value = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Endpoint_DeprecatedTopologyEntry {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : '',
            value: isSet(object.value) ? globalThis.String(object.value) : '',
        };
    },

    toJSON(message: Endpoint_DeprecatedTopologyEntry): unknown {
        const obj: any = {};
        if (message.key !== '') {
            obj.key = message.key;
        }
        if (message.value !== '') {
            obj.value = message.value;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Endpoint_DeprecatedTopologyEntry>, I>>(
        base?: I,
    ): Endpoint_DeprecatedTopologyEntry {
        return Endpoint_DeprecatedTopologyEntry.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Endpoint_DeprecatedTopologyEntry>, I>>(
        object: I,
    ): Endpoint_DeprecatedTopologyEntry {
        const message = createBaseEndpoint_DeprecatedTopologyEntry();
        message.key = object.key ?? '';
        message.value = object.value ?? '';
        return message;
    },
};

function createBaseEndpointConditions(): EndpointConditions {
    return { ready: false, serving: false, terminating: false };
}

export const EndpointConditions: MessageFns<EndpointConditions> = {
    encode(message: EndpointConditions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.ready !== undefined && message.ready !== false) {
            writer.uint32(8).bool(message.ready);
        }
        if (message.serving !== undefined && message.serving !== false) {
            writer.uint32(16).bool(message.serving);
        }
        if (message.terminating !== undefined && message.terminating !== false) {
            writer.uint32(24).bool(message.terminating);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): EndpointConditions {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpointConditions();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 8) {
                            break;
                        }

                        message.ready = reader.bool();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }

                        message.serving = reader.bool();
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }

                        message.terminating = reader.bool();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): EndpointConditions {
        return {
            ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
            serving: isSet(object.serving) ? globalThis.Boolean(object.serving) : false,
            terminating: isSet(object.terminating) ? globalThis.Boolean(object.terminating) : false,
        };
    },

    toJSON(message: EndpointConditions): unknown {
        const obj: any = {};
        if (message.ready !== undefined && message.ready !== false) {
            obj.ready = message.ready;
        }
        if (message.serving !== undefined && message.serving !== false) {
            obj.serving = message.serving;
        }
        if (message.terminating !== undefined && message.terminating !== false) {
            obj.terminating = message.terminating;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<EndpointConditions>, I>>(base?: I): EndpointConditions {
        return EndpointConditions.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<EndpointConditions>, I>>(object: I): EndpointConditions {
        const message = createBaseEndpointConditions();
        message.ready = object.ready ?? false;
        message.serving = object.serving ?? false;
        message.terminating = object.terminating ?? false;
        return message;
    },
};

function createBaseEndpointHints(): EndpointHints {
    return { forZones: [], forNodes: [] };
}

export const EndpointHints: MessageFns<EndpointHints> = {
    encode(message: EndpointHints, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        for (const v of message.forZones) {
            ForZone.encode(v!, writer.uint32(10).fork()).join();
        }
        for (const v of message.forNodes) {
            ForNode.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): EndpointHints {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpointHints();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.forZones.push(ForZone.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.forNodes.push(ForNode.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): EndpointHints {
        return {
            forZones: globalThis.Array.isArray(object?.forZones)
                ? object.forZones.map((e: any) => ForZone.fromJSON(e))
                : [],
            forNodes: globalThis.Array.isArray(object?.forNodes)
                ? object.forNodes.map((e: any) => ForNode.fromJSON(e))
                : [],
        };
    },

    toJSON(message: EndpointHints): unknown {
        const obj: any = {};
        if (message.forZones?.length) {
            obj.forZones = message.forZones.map((e) => ForZone.toJSON(e));
        }
        if (message.forNodes?.length) {
            obj.forNodes = message.forNodes.map((e) => ForNode.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<EndpointHints>, I>>(base?: I): EndpointHints {
        return EndpointHints.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<EndpointHints>, I>>(object: I): EndpointHints {
        const message = createBaseEndpointHints();
        message.forZones = object.forZones?.map((e) => ForZone.fromPartial(e)) || [];
        message.forNodes = object.forNodes?.map((e) => ForNode.fromPartial(e)) || [];
        return message;
    },
};

function createBaseEndpointPort(): EndpointPort {
    return { name: '', protocol: '', port: 0, appProtocol: '' };
}

export const EndpointPort: MessageFns<EndpointPort> = {
    encode(message: EndpointPort, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.name !== undefined && message.name !== '') {
            writer.uint32(10).string(message.name);
        }
        if (message.protocol !== undefined && message.protocol !== '') {
            writer.uint32(18).string(message.protocol);
        }
        if (message.port !== undefined && message.port !== 0) {
            writer.uint32(24).int32(message.port);
        }
        if (message.appProtocol !== undefined && message.appProtocol !== '') {
            writer.uint32(34).string(message.appProtocol);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): EndpointPort {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpointPort();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.name = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.protocol = reader.string();
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }

                        message.port = reader.int32();
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.appProtocol = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): EndpointPort {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : '',
            protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : '',
            port: isSet(object.port) ? globalThis.Number(object.port) : 0,
            appProtocol: isSet(object.appProtocol) ? globalThis.String(object.appProtocol) : '',
        };
    },

    toJSON(message: EndpointPort): unknown {
        const obj: any = {};
        if (message.name !== undefined && message.name !== '') {
            obj.name = message.name;
        }
        if (message.protocol !== undefined && message.protocol !== '') {
            obj.protocol = message.protocol;
        }
        if (message.port !== undefined && message.port !== 0) {
            obj.port = Math.round(message.port);
        }
        if (message.appProtocol !== undefined && message.appProtocol !== '') {
            obj.appProtocol = message.appProtocol;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<EndpointPort>, I>>(base?: I): EndpointPort {
        return EndpointPort.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<EndpointPort>, I>>(object: I): EndpointPort {
        const message = createBaseEndpointPort();
        message.name = object.name ?? '';
        message.protocol = object.protocol ?? '';
        message.port = object.port ?? 0;
        message.appProtocol = object.appProtocol ?? '';
        return message;
    },
};

function createBaseEndpointSlice(): EndpointSlice {
    return { metadata: undefined, addressType: '', endpoints: [], ports: [] };
}

export const EndpointSlice: MessageFns<EndpointSlice> = {
    encode(message: EndpointSlice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.addressType !== undefined && message.addressType !== '') {
            writer.uint32(34).string(message.addressType);
        }
        for (const v of message.endpoints) {
            Endpoint.encode(v!, writer.uint32(18).fork()).join();
        }
        for (const v of message.ports) {
            EndpointPort.encode(v!, writer.uint32(26).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): EndpointSlice {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpointSlice();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.addressType = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.endpoints.push(Endpoint.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.ports.push(EndpointPort.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): EndpointSlice {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            addressType: isSet(object.addressType) ? globalThis.String(object.addressType) : '',
            endpoints: globalThis.Array.isArray(object?.endpoints)
                ? object.endpoints.map((e: any) => Endpoint.fromJSON(e))
                : [],
            ports: globalThis.Array.isArray(object?.ports)
                ? object.ports.map((e: any) => EndpointPort.fromJSON(e))
                : [],
        };
    },

    toJSON(message: EndpointSlice): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.addressType !== undefined && message.addressType !== '') {
            obj.addressType = message.addressType;
        }
        if (message.endpoints?.length) {
            obj.endpoints = message.endpoints.map((e) => Endpoint.toJSON(e));
        }
        if (message.ports?.length) {
            obj.ports = message.ports.map((e) => EndpointPort.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<EndpointSlice>, I>>(base?: I): EndpointSlice {
        return EndpointSlice.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<EndpointSlice>, I>>(object: I): EndpointSlice {
        const message = createBaseEndpointSlice();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.addressType = object.addressType ?? '';
        message.endpoints = object.endpoints?.map((e) => Endpoint.fromPartial(e)) || [];
        message.ports = object.ports?.map((e) => EndpointPort.fromPartial(e)) || [];
        return message;
    },
};

function createBaseEndpointSliceList(): EndpointSliceList {
    return { metadata: undefined, items: [] };
}

export const EndpointSliceList: MessageFns<EndpointSliceList> = {
    encode(message: EndpointSliceList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            EndpointSlice.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): EndpointSliceList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseEndpointSliceList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(EndpointSlice.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): EndpointSliceList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => EndpointSlice.fromJSON(e))
                : [],
        };
    },

    toJSON(message: EndpointSliceList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => EndpointSlice.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<EndpointSliceList>, I>>(base?: I): EndpointSliceList {
        return EndpointSliceList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<EndpointSliceList>, I>>(object: I): EndpointSliceList {
        const message = createBaseEndpointSliceList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => EndpointSlice.fromPartial(e)) || [];
        return message;
    },
};

function createBaseForNode(): ForNode {
    return { name: '' };
}

export const ForNode: MessageFns<ForNode> = {
    encode(message: ForNode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.name !== undefined && message.name !== '') {
            writer.uint32(10).string(message.name);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ForNode {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseForNode();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.name = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ForNode {
        return { name: isSet(object.name) ? globalThis.String(object.name) : '' };
    },

    toJSON(message: ForNode): unknown {
        const obj: any = {};
        if (message.name !== undefined && message.name !== '') {
            obj.name = message.name;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ForNode>, I>>(base?: I): ForNode {
        return ForNode.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ForNode>, I>>(object: I): ForNode {
        const message = createBaseForNode();
        message.name = object.name ?? '';
        return message;
    },
};

function createBaseForZone(): ForZone {
    return { name: '' };
}

export const ForZone: MessageFns<ForZone> = {
    encode(message: ForZone, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.name !== undefined && message.name !== '') {
            writer.uint32(10).string(message.name);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): ForZone {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseForZone();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.name = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): ForZone {
        return { name: isSet(object.name) ? globalThis.String(object.name) : '' };
    },

    toJSON(message: ForZone): unknown {
        const obj: any = {};
        if (message.name !== undefined && message.name !== '') {
            obj.name = message.name;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<ForZone>, I>>(base?: I): ForZone {
        return ForZone.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<ForZone>, I>>(object: I): ForZone {
        const message = createBaseForZone();
        message.name = object.name ?? '';
        return message;
    },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
    ? T
    : T extends globalThis.Array<infer U>
      ? globalThis.Array<DeepPartial<U>>
      : T extends ReadonlyArray<infer U>
        ? ReadonlyArray<DeepPartial<U>>
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
    ? P
    : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
    return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
    return value !== null && value !== undefined;
}

interface MessageFns<T> {
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
    fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
