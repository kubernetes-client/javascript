// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v3.6.1
// source: k8s.io/api/node/v1/generated.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { Quantity } from '../../../apimachinery/pkg/api/resource/generated.js';
import { ListMeta, ObjectMeta } from '../../../apimachinery/pkg/apis/meta/v1/generated.js';
import { Toleration } from '../../core/v1/generated.js';

/** Overhead structure represents the resource overhead associated with running a pod. */
export interface Overhead {
    /**
     * podFixed represents the fixed resource overhead associated with running a pod.
     * +optional
     */
    podFixed: { [key: string]: Quantity };
}

export interface Overhead_PodFixedEntry {
    key: string;
    value: Quantity | undefined;
}

/**
 * RuntimeClass defines a class of container runtime supported in the cluster.
 * The RuntimeClass is used to determine which container runtime is used to run
 * all containers in a pod. RuntimeClasses are manually defined by a
 * user or cluster provisioner, and referenced in the PodSpec. The Kubelet is
 * responsible for resolving the RuntimeClassName reference before running the
 * pod.  For more details, see
 * https://kubernetes.io/docs/concepts/containers/runtime-class/
 */
export interface RuntimeClass {
    /**
     * metadata is the standard object metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ObjectMeta | undefined;
    /**
     * handler specifies the underlying runtime and configuration that the CRI
     * implementation will use to handle pods of this class. The possible values
     * are specific to the node & CRI configuration.  It is assumed that all
     * handlers are available on every node, and handlers of the same name are
     * equivalent on every node.
     * For example, a handler called "runc" might specify that the runc OCI
     * runtime (using native Linux containers) will be used to run the containers
     * in a pod.
     * The Handler must be lowercase, conform to the DNS Label (RFC 1123) requirements,
     * and is immutable.
     * +required
     * +k8s:beta(since: "1.37")=+k8s:format="k8s-short-name"
     * +k8s:beta(since: "1.37")=+k8s:immutable
     * +k8s:beta(since: "1.37")=+k8s:required
     */
    handler?: string | undefined;
    /**
     * overhead represents the resource overhead associated with running a pod for a
     * given RuntimeClass. For more details, see
     *  https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/
     * +optional
     */
    overhead?: Overhead | undefined;
    /**
     * scheduling holds the scheduling constraints to ensure that pods running
     * with this RuntimeClass are scheduled to nodes that support it.
     * If scheduling is nil, this RuntimeClass is assumed to be supported by all
     * nodes.
     * +optional
     * +k8s:alpha(since: "1.37")=+k8s:optional
     */
    scheduling?: Scheduling | undefined;
}

/** RuntimeClassList is a list of RuntimeClass objects. */
export interface RuntimeClassList {
    /**
     * Standard list metadata.
     * More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
     * +optional
     */
    metadata?: ListMeta | undefined;
    /** items is a list of schema objects. */
    items: RuntimeClass[];
}

/**
 * Scheduling specifies the scheduling constraints for nodes supporting a
 * RuntimeClass.
 */
export interface Scheduling {
    /**
     * nodeSelector lists labels that must be present on nodes that support this
     * RuntimeClass. Pods using this RuntimeClass can only be scheduled to a
     * node matched by this selector. The RuntimeClass nodeSelector is merged
     * with a pod's existing nodeSelector. Any conflicts will cause the pod to
     * be rejected in admission.
     * +optional
     * +mapType=atomic
     */
    nodeSelector: { [key: string]: string };
    /**
     * tolerations are appended (excluding duplicates) to pods running with this
     * RuntimeClass during admission, effectively unioning the set of nodes
     * tolerated by the pod and the RuntimeClass.
     * +optional
     * +listType=atomic
     * +k8s:alpha(since: "1.37")=+k8s:optional
     */
    tolerations: Toleration[];
}

export interface Scheduling_NodeSelectorEntry {
    key: string;
    value: string;
}

function createBaseOverhead(): Overhead {
    return { podFixed: {} };
}

export const Overhead: MessageFns<Overhead> = {
    encode(message: Overhead, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        globalThis.Object.entries(message.podFixed).forEach(([key, value]: [string, Quantity]) => {
            Overhead_PodFixedEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
        });
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Overhead {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseOverhead();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        const entry1 = Overhead_PodFixedEntry.decode(reader, reader.uint32());
                        if (entry1.value !== undefined) {
                            message.podFixed[entry1.key] = entry1.value;
                        }
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Overhead {
        return {
            podFixed: isObject(object.podFixed)
                ? (globalThis.Object.entries(object.podFixed) as [string, any][]).reduce(
                      (acc: { [key: string]: Quantity }, [key, value]: [string, any]) => {
                          globalThis.Object.defineProperty(acc, key, {
                              value: Quantity.fromJSON(value),
                              enumerable: true,
                              configurable: true,
                              writable: true,
                          });
                          return acc;
                      },
                      {},
                  )
                : {},
        };
    },

    toJSON(message: Overhead): unknown {
        const obj: any = {};
        if (message.podFixed) {
            const entries = globalThis.Object.entries(message.podFixed) as [string, Quantity][];
            if (entries.length > 0) {
                obj.podFixed = {};
                entries.forEach(([k, v]) => {
                    obj.podFixed[k] = Quantity.toJSON(v);
                });
            }
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Overhead>, I>>(base?: I): Overhead {
        return Overhead.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Overhead>, I>>(object: I): Overhead {
        const message = createBaseOverhead();
        message.podFixed = (globalThis.Object.entries(object.podFixed ?? {}) as [string, Quantity][]).reduce(
            (acc: { [key: string]: Quantity }, [key, value]: [string, Quantity]) => {
                if (value !== undefined) {
                    acc[key] = Quantity.fromPartial(value);
                }
                return acc;
            },
            {},
        );
        return message;
    },
};

function createBaseOverhead_PodFixedEntry(): Overhead_PodFixedEntry {
    return { key: '', value: undefined };
}

export const Overhead_PodFixedEntry: MessageFns<Overhead_PodFixedEntry> = {
    encode(message: Overhead_PodFixedEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.key !== '') {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            Quantity.encode(message.value, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Overhead_PodFixedEntry {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseOverhead_PodFixedEntry();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.key = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.value = Quantity.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Overhead_PodFixedEntry {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : '',
            value: isSet(object.value) ? Quantity.fromJSON(object.value) : undefined,
        };
    },

    toJSON(message: Overhead_PodFixedEntry): unknown {
        const obj: any = {};
        if (message.key !== '') {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = Quantity.toJSON(message.value);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Overhead_PodFixedEntry>, I>>(base?: I): Overhead_PodFixedEntry {
        return Overhead_PodFixedEntry.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Overhead_PodFixedEntry>, I>>(object: I): Overhead_PodFixedEntry {
        const message = createBaseOverhead_PodFixedEntry();
        message.key = object.key ?? '';
        message.value =
            object.value !== undefined && object.value !== null
                ? Quantity.fromPartial(object.value)
                : undefined;
        return message;
    },
};

function createBaseRuntimeClass(): RuntimeClass {
    return { metadata: undefined, handler: '', overhead: undefined, scheduling: undefined };
}

export const RuntimeClass: MessageFns<RuntimeClass> = {
    encode(message: RuntimeClass, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ObjectMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.handler !== undefined && message.handler !== '') {
            writer.uint32(18).string(message.handler);
        }
        if (message.overhead !== undefined) {
            Overhead.encode(message.overhead, writer.uint32(26).fork()).join();
        }
        if (message.scheduling !== undefined) {
            Scheduling.encode(message.scheduling, writer.uint32(34).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): RuntimeClass {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseRuntimeClass();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ObjectMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.handler = reader.string();
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }

                        message.overhead = Overhead.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }

                        message.scheduling = Scheduling.decode(reader, reader.uint32());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): RuntimeClass {
        return {
            metadata: isSet(object.metadata) ? ObjectMeta.fromJSON(object.metadata) : undefined,
            handler: isSet(object.handler) ? globalThis.String(object.handler) : '',
            overhead: isSet(object.overhead) ? Overhead.fromJSON(object.overhead) : undefined,
            scheduling: isSet(object.scheduling) ? Scheduling.fromJSON(object.scheduling) : undefined,
        };
    },

    toJSON(message: RuntimeClass): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ObjectMeta.toJSON(message.metadata);
        }
        if (message.handler !== undefined && message.handler !== '') {
            obj.handler = message.handler;
        }
        if (message.overhead !== undefined) {
            obj.overhead = Overhead.toJSON(message.overhead);
        }
        if (message.scheduling !== undefined) {
            obj.scheduling = Scheduling.toJSON(message.scheduling);
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<RuntimeClass>, I>>(base?: I): RuntimeClass {
        return RuntimeClass.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<RuntimeClass>, I>>(object: I): RuntimeClass {
        const message = createBaseRuntimeClass();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ObjectMeta.fromPartial(object.metadata)
                : undefined;
        message.handler = object.handler ?? '';
        message.overhead =
            object.overhead !== undefined && object.overhead !== null
                ? Overhead.fromPartial(object.overhead)
                : undefined;
        message.scheduling =
            object.scheduling !== undefined && object.scheduling !== null
                ? Scheduling.fromPartial(object.scheduling)
                : undefined;
        return message;
    },
};

function createBaseRuntimeClassList(): RuntimeClassList {
    return { metadata: undefined, items: [] };
}

export const RuntimeClassList: MessageFns<RuntimeClassList> = {
    encode(message: RuntimeClassList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.metadata !== undefined) {
            ListMeta.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            RuntimeClass.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): RuntimeClassList {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseRuntimeClassList();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.metadata = ListMeta.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.items.push(RuntimeClass.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): RuntimeClassList {
        return {
            metadata: isSet(object.metadata) ? ListMeta.fromJSON(object.metadata) : undefined,
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e: any) => RuntimeClass.fromJSON(e))
                : [],
        };
    },

    toJSON(message: RuntimeClassList): unknown {
        const obj: any = {};
        if (message.metadata !== undefined) {
            obj.metadata = ListMeta.toJSON(message.metadata);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => RuntimeClass.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<RuntimeClassList>, I>>(base?: I): RuntimeClassList {
        return RuntimeClassList.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<RuntimeClassList>, I>>(object: I): RuntimeClassList {
        const message = createBaseRuntimeClassList();
        message.metadata =
            object.metadata !== undefined && object.metadata !== null
                ? ListMeta.fromPartial(object.metadata)
                : undefined;
        message.items = object.items?.map((e) => RuntimeClass.fromPartial(e)) || [];
        return message;
    },
};

function createBaseScheduling(): Scheduling {
    return { nodeSelector: {}, tolerations: [] };
}

export const Scheduling: MessageFns<Scheduling> = {
    encode(message: Scheduling, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        globalThis.Object.entries(message.nodeSelector).forEach(([key, value]: [string, string]) => {
            Scheduling_NodeSelectorEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
        });
        for (const v of message.tolerations) {
            Toleration.encode(v!, writer.uint32(18).fork()).join();
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Scheduling {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseScheduling();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        const entry1 = Scheduling_NodeSelectorEntry.decode(reader, reader.uint32());
                        if (entry1.value !== undefined) {
                            message.nodeSelector[entry1.key] = entry1.value;
                        }
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.tolerations.push(Toleration.decode(reader, reader.uint32()));
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Scheduling {
        return {
            nodeSelector: isObject(object.nodeSelector)
                ? (globalThis.Object.entries(object.nodeSelector) as [string, any][]).reduce(
                      (acc: { [key: string]: string }, [key, value]: [string, any]) => {
                          globalThis.Object.defineProperty(acc, key, {
                              value: globalThis.String(value),
                              enumerable: true,
                              configurable: true,
                              writable: true,
                          });
                          return acc;
                      },
                      {},
                  )
                : {},
            tolerations: globalThis.Array.isArray(object?.tolerations)
                ? object.tolerations.map((e: any) => Toleration.fromJSON(e))
                : [],
        };
    },

    toJSON(message: Scheduling): unknown {
        const obj: any = {};
        if (message.nodeSelector) {
            const entries = globalThis.Object.entries(message.nodeSelector) as [string, string][];
            if (entries.length > 0) {
                obj.nodeSelector = {};
                entries.forEach(([k, v]) => {
                    obj.nodeSelector[k] = v;
                });
            }
        }
        if (message.tolerations?.length) {
            obj.tolerations = message.tolerations.map((e) => Toleration.toJSON(e));
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Scheduling>, I>>(base?: I): Scheduling {
        return Scheduling.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Scheduling>, I>>(object: I): Scheduling {
        const message = createBaseScheduling();
        message.nodeSelector = (
            globalThis.Object.entries(object.nodeSelector ?? {}) as [string, string][]
        ).reduce((acc: { [key: string]: string }, [key, value]: [string, string]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        message.tolerations = object.tolerations?.map((e) => Toleration.fromPartial(e)) || [];
        return message;
    },
};

function createBaseScheduling_NodeSelectorEntry(): Scheduling_NodeSelectorEntry {
    return { key: '', value: '' };
}

export const Scheduling_NodeSelectorEntry: MessageFns<Scheduling_NodeSelectorEntry> = {
    encode(message: Scheduling_NodeSelectorEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
        if (message.key !== '') {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== '') {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): Scheduling_NodeSelectorEntry {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error('protobuf decode recursion limit exceeded');
        }
        (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseScheduling_NodeSelectorEntry();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }

                        message.key = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }

                        message.value = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        } finally {
            (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },

    fromJSON(object: any): Scheduling_NodeSelectorEntry {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : '',
            value: isSet(object.value) ? globalThis.String(object.value) : '',
        };
    },

    toJSON(message: Scheduling_NodeSelectorEntry): unknown {
        const obj: any = {};
        if (message.key !== '') {
            obj.key = message.key;
        }
        if (message.value !== '') {
            obj.value = message.value;
        }
        return obj;
    },

    create<I extends Exact<DeepPartial<Scheduling_NodeSelectorEntry>, I>>(
        base?: I,
    ): Scheduling_NodeSelectorEntry {
        return Scheduling_NodeSelectorEntry.fromPartial(base ?? ({} as any));
    },
    fromPartial<I extends Exact<DeepPartial<Scheduling_NodeSelectorEntry>, I>>(
        object: I,
    ): Scheduling_NodeSelectorEntry {
        const message = createBaseScheduling_NodeSelectorEntry();
        message.key = object.key ?? '';
        message.value = object.value ?? '';
        return message;
    },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
    ? T
    : T extends globalThis.Array<infer U>
      ? globalThis.Array<DeepPartial<U>>
      : T extends ReadonlyArray<infer U>
        ? ReadonlyArray<DeepPartial<U>>
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
    ? P
    : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
    return typeof value === 'object' && value !== null;
}

function isSet(value: any): boolean {
    return value !== null && value !== undefined;
}

interface MessageFns<T> {
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
    fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
