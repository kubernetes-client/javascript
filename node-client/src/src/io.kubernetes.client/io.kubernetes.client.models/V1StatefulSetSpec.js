/**
 * Kubernetes
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: v1.10.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 *
 */


import ApiClient from '../ApiClient';
import V1LabelSelector from './V1LabelSelector';
import V1PersistentVolumeClaim from './V1PersistentVolumeClaim';
import V1PodTemplateSpec from './V1PodTemplateSpec';
import V1StatefulSetUpdateStrategy from './V1StatefulSetUpdateStrategy';





/**
* The V1StatefulSetSpec model module.
* @module io.kubernetes.client/io.kubernetes.client.models/V1StatefulSetSpec
* @version 0.3-SNAPSHOT
*/
export default class V1StatefulSetSpec {
    /**
    * Constructs a new <code>V1StatefulSetSpec</code>.
    * A StatefulSetSpec is the specification of a StatefulSet.
    * @alias module:io.kubernetes.client/io.kubernetes.client.models/V1StatefulSetSpec
    * @class
    * @param selector {module:io.kubernetes.client/io.kubernetes.client.models/V1LabelSelector} selector is a label query over pods that should match the replica count. It must match the pod template's labels. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
    * @param serviceName {String} serviceName is the name of the service that governs this StatefulSet. This service must exist before the StatefulSet, and is responsible for the network identity of the set. Pods get DNS/hostnames that follow the pattern: pod-specific-string.serviceName.default.svc.cluster.local where \"pod-specific-string\" is managed by the StatefulSet controller.
    * @param template {module:io.kubernetes.client/io.kubernetes.client.models/V1PodTemplateSpec} template is the object that describes the pod that will be created if insufficient replicas are detected. Each pod stamped out by the StatefulSet will fulfill this Template, but have a unique identity from the rest of the StatefulSet.
    */

    constructor(selector, serviceName, template) {
        

        
        

        this['selector'] = selector;this['serviceName'] = serviceName;this['template'] = template;

        
    }

    /**
    * Constructs a <code>V1StatefulSetSpec</code> from a plain JavaScript object, optionally creating a new instance.
    * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
    * @param {Object} data The plain JavaScript object bearing properties of interest.
    * @param {module:io.kubernetes.client/io.kubernetes.client.models/V1StatefulSetSpec} obj Optional instance to populate.
    * @return {module:io.kubernetes.client/io.kubernetes.client.models/V1StatefulSetSpec} The populated <code>V1StatefulSetSpec</code> instance.
    */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V1StatefulSetSpec();

            
            
            

            if (data.hasOwnProperty('podManagementPolicy')) {
                obj['podManagementPolicy'] = ApiClient.convertToType(data['podManagementPolicy'], 'String');
            }
            if (data.hasOwnProperty('replicas')) {
                obj['replicas'] = ApiClient.convertToType(data['replicas'], 'Number');
            }
            if (data.hasOwnProperty('revisionHistoryLimit')) {
                obj['revisionHistoryLimit'] = ApiClient.convertToType(data['revisionHistoryLimit'], 'Number');
            }
            if (data.hasOwnProperty('selector')) {
                obj['selector'] = V1LabelSelector.constructFromObject(data['selector']);
            }
            if (data.hasOwnProperty('serviceName')) {
                obj['serviceName'] = ApiClient.convertToType(data['serviceName'], 'String');
            }
            if (data.hasOwnProperty('template')) {
                obj['template'] = V1PodTemplateSpec.constructFromObject(data['template']);
            }
            if (data.hasOwnProperty('updateStrategy')) {
                obj['updateStrategy'] = V1StatefulSetUpdateStrategy.constructFromObject(data['updateStrategy']);
            }
            if (data.hasOwnProperty('volumeClaimTemplates')) {
                obj['volumeClaimTemplates'] = ApiClient.convertToType(data['volumeClaimTemplates'], [V1PersistentVolumeClaim]);
            }
        }
        return obj;
    }

    /**
    * podManagementPolicy controls how pods are created during initial scale up, when replacing pods on nodes, or when scaling down. The default policy is `OrderedReady`, where pods are created in increasing order (pod-0, then pod-1, etc) and the controller will wait until each pod is ready before continuing. When scaling down, the pods are removed in the opposite order. The alternative policy is `Parallel` which will create pods in parallel to match the desired scale without waiting, and on scale down will delete all pods at once.
    * @member {String} podManagementPolicy
    */
    podManagementPolicy = undefined;
    /**
    * replicas is the desired number of replicas of the given Template. These are replicas in the sense that they are instantiations of the same Template, but individual replicas also have a consistent identity. If unspecified, defaults to 1.
    * @member {Number} replicas
    */
    replicas = undefined;
    /**
    * revisionHistoryLimit is the maximum number of revisions that will be maintained in the StatefulSet's revision history. The revision history consists of all revisions not represented by a currently applied StatefulSetSpec version. The default value is 10.
    * @member {Number} revisionHistoryLimit
    */
    revisionHistoryLimit = undefined;
    /**
    * selector is a label query over pods that should match the replica count. It must match the pod template's labels. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
    * @member {module:io.kubernetes.client/io.kubernetes.client.models/V1LabelSelector} selector
    */
    selector = undefined;
    /**
    * serviceName is the name of the service that governs this StatefulSet. This service must exist before the StatefulSet, and is responsible for the network identity of the set. Pods get DNS/hostnames that follow the pattern: pod-specific-string.serviceName.default.svc.cluster.local where \"pod-specific-string\" is managed by the StatefulSet controller.
    * @member {String} serviceName
    */
    serviceName = undefined;
    /**
    * template is the object that describes the pod that will be created if insufficient replicas are detected. Each pod stamped out by the StatefulSet will fulfill this Template, but have a unique identity from the rest of the StatefulSet.
    * @member {module:io.kubernetes.client/io.kubernetes.client.models/V1PodTemplateSpec} template
    */
    template = undefined;
    /**
    * updateStrategy indicates the StatefulSetUpdateStrategy that will be employed to update Pods in the StatefulSet when a revision is made to Template.
    * @member {module:io.kubernetes.client/io.kubernetes.client.models/V1StatefulSetUpdateStrategy} updateStrategy
    */
    updateStrategy = undefined;
    /**
    * volumeClaimTemplates is a list of claims that pods are allowed to reference. The StatefulSet controller is responsible for mapping network identities to claims in a way that maintains the identity of a pod. Every claim in this list must have at least one matching (by name) volumeMount in one container in the template. A claim in this list takes precedence over any volumes in the template, with the same name.
    * @member {Array.<module:io.kubernetes.client/io.kubernetes.client.models/V1PersistentVolumeClaim>} volumeClaimTemplates
    */
    volumeClaimTemplates = undefined;








}


