/**
 * Kubernetes
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: v1.10.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 *
 */


import ApiClient from '../ApiClient';
import V2beta1CrossVersionObjectReference from './V2beta1CrossVersionObjectReference';
import V2beta1MetricSpec from './V2beta1MetricSpec';





/**
* The V2beta1HorizontalPodAutoscalerSpec model module.
* @module io.kubernetes.client/io.kubernetes.client.models/V2beta1HorizontalPodAutoscalerSpec
* @version 0.3-SNAPSHOT
*/
export default class V2beta1HorizontalPodAutoscalerSpec {
    /**
    * Constructs a new <code>V2beta1HorizontalPodAutoscalerSpec</code>.
    * HorizontalPodAutoscalerSpec describes the desired functionality of the HorizontalPodAutoscaler.
    * @alias module:io.kubernetes.client/io.kubernetes.client.models/V2beta1HorizontalPodAutoscalerSpec
    * @class
    * @param maxReplicas {Number} maxReplicas is the upper limit for the number of replicas to which the autoscaler can scale up. It cannot be less that minReplicas.
    * @param scaleTargetRef {module:io.kubernetes.client/io.kubernetes.client.models/V2beta1CrossVersionObjectReference} scaleTargetRef points to the target resource to scale, and is used to the pods for which metrics should be collected, as well as to actually change the replica count.
    */

    constructor(maxReplicas, scaleTargetRef) {
        

        
        

        this['maxReplicas'] = maxReplicas;this['scaleTargetRef'] = scaleTargetRef;

        
    }

    /**
    * Constructs a <code>V2beta1HorizontalPodAutoscalerSpec</code> from a plain JavaScript object, optionally creating a new instance.
    * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
    * @param {Object} data The plain JavaScript object bearing properties of interest.
    * @param {module:io.kubernetes.client/io.kubernetes.client.models/V2beta1HorizontalPodAutoscalerSpec} obj Optional instance to populate.
    * @return {module:io.kubernetes.client/io.kubernetes.client.models/V2beta1HorizontalPodAutoscalerSpec} The populated <code>V2beta1HorizontalPodAutoscalerSpec</code> instance.
    */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V2beta1HorizontalPodAutoscalerSpec();

            
            
            

            if (data.hasOwnProperty('maxReplicas')) {
                obj['maxReplicas'] = ApiClient.convertToType(data['maxReplicas'], 'Number');
            }
            if (data.hasOwnProperty('metrics')) {
                obj['metrics'] = ApiClient.convertToType(data['metrics'], [V2beta1MetricSpec]);
            }
            if (data.hasOwnProperty('minReplicas')) {
                obj['minReplicas'] = ApiClient.convertToType(data['minReplicas'], 'Number');
            }
            if (data.hasOwnProperty('scaleTargetRef')) {
                obj['scaleTargetRef'] = V2beta1CrossVersionObjectReference.constructFromObject(data['scaleTargetRef']);
            }
        }
        return obj;
    }

    /**
    * maxReplicas is the upper limit for the number of replicas to which the autoscaler can scale up. It cannot be less that minReplicas.
    * @member {Number} maxReplicas
    */
    maxReplicas = undefined;
    /**
    * metrics contains the specifications for which to use to calculate the desired replica count (the maximum replica count across all metrics will be used).  The desired replica count is calculated multiplying the ratio between the target value and the current value by the current number of pods.  Ergo, metrics used must decrease as the pod count is increased, and vice-versa.  See the individual metric source types for more information about how each type of metric must respond.
    * @member {Array.<module:io.kubernetes.client/io.kubernetes.client.models/V2beta1MetricSpec>} metrics
    */
    metrics = undefined;
    /**
    * minReplicas is the lower limit for the number of replicas to which the autoscaler can scale down. It defaults to 1 pod.
    * @member {Number} minReplicas
    */
    minReplicas = undefined;
    /**
    * scaleTargetRef points to the target resource to scale, and is used to the pods for which metrics should be collected, as well as to actually change the replica count.
    * @member {module:io.kubernetes.client/io.kubernetes.client.models/V2beta1CrossVersionObjectReference} scaleTargetRef
    */
    scaleTargetRef = undefined;








}


