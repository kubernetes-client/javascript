/**
 * Kubernetes
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: v1.10.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 *
 */


import ApiClient from '../ApiClient';
import V1LabelSelector from './V1LabelSelector';
import V1beta1NetworkPolicyEgressRule from './V1beta1NetworkPolicyEgressRule';
import V1beta1NetworkPolicyIngressRule from './V1beta1NetworkPolicyIngressRule';





/**
* The V1beta1NetworkPolicySpec model module.
* @module io.kubernetes.client/io.kubernetes.client.models/V1beta1NetworkPolicySpec
* @version 0.3-SNAPSHOT
*/
export default class V1beta1NetworkPolicySpec {
    /**
    * Constructs a new <code>V1beta1NetworkPolicySpec</code>.
    * DEPRECATED 1.9 - This group version of NetworkPolicySpec is deprecated by networking/v1/NetworkPolicySpec.
    * @alias module:io.kubernetes.client/io.kubernetes.client.models/V1beta1NetworkPolicySpec
    * @class
    * @param podSelector {module:io.kubernetes.client/io.kubernetes.client.models/V1LabelSelector} Selects the pods to which this NetworkPolicy object applies.  The array of ingress rules is applied to any pods selected by this field. Multiple network policies can select the same set of pods.  In this case, the ingress rules for each are combined additively. This field is NOT optional and follows standard label selector semantics. An empty podSelector matches all pods in this namespace.
    */

    constructor(podSelector) {
        

        
        

        this['podSelector'] = podSelector;

        
    }

    /**
    * Constructs a <code>V1beta1NetworkPolicySpec</code> from a plain JavaScript object, optionally creating a new instance.
    * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
    * @param {Object} data The plain JavaScript object bearing properties of interest.
    * @param {module:io.kubernetes.client/io.kubernetes.client.models/V1beta1NetworkPolicySpec} obj Optional instance to populate.
    * @return {module:io.kubernetes.client/io.kubernetes.client.models/V1beta1NetworkPolicySpec} The populated <code>V1beta1NetworkPolicySpec</code> instance.
    */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V1beta1NetworkPolicySpec();

            
            
            

            if (data.hasOwnProperty('egress')) {
                obj['egress'] = ApiClient.convertToType(data['egress'], [V1beta1NetworkPolicyEgressRule]);
            }
            if (data.hasOwnProperty('ingress')) {
                obj['ingress'] = ApiClient.convertToType(data['ingress'], [V1beta1NetworkPolicyIngressRule]);
            }
            if (data.hasOwnProperty('podSelector')) {
                obj['podSelector'] = V1LabelSelector.constructFromObject(data['podSelector']);
            }
            if (data.hasOwnProperty('policyTypes')) {
                obj['policyTypes'] = ApiClient.convertToType(data['policyTypes'], ['String']);
            }
        }
        return obj;
    }

    /**
    * List of egress rules to be applied to the selected pods. Outgoing traffic is allowed if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic matches at least one egress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy limits all outgoing traffic (and serves solely to ensure that the pods it selects are isolated by default). This field is beta-level in 1.8
    * @member {Array.<module:io.kubernetes.client/io.kubernetes.client.models/V1beta1NetworkPolicyEgressRule>} egress
    */
    egress = undefined;
    /**
    * List of ingress rules to be applied to the selected pods. Traffic is allowed to a pod if there are no NetworkPolicies selecting the pod OR if the traffic source is the pod's local node, OR if the traffic matches at least one ingress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy does not allow any traffic (and serves solely to ensure that the pods it selects are isolated by default).
    * @member {Array.<module:io.kubernetes.client/io.kubernetes.client.models/V1beta1NetworkPolicyIngressRule>} ingress
    */
    ingress = undefined;
    /**
    * Selects the pods to which this NetworkPolicy object applies.  The array of ingress rules is applied to any pods selected by this field. Multiple network policies can select the same set of pods.  In this case, the ingress rules for each are combined additively. This field is NOT optional and follows standard label selector semantics. An empty podSelector matches all pods in this namespace.
    * @member {module:io.kubernetes.client/io.kubernetes.client.models/V1LabelSelector} podSelector
    */
    podSelector = undefined;
    /**
    * List of rule types that the NetworkPolicy relates to. Valid options are Ingress, Egress, or Ingress,Egress. If this field is not specified, it will default based on the existence of Ingress or Egress rules; policies that contain an Egress section are assumed to affect Egress, and all policies (whether or not they contain an Ingress section) are assumed to affect Ingress. If you want to write an egress-only policy, you must explicitly specify policyTypes [ \"Egress\" ]. Likewise, if you want to write a policy that specifies that no egress is allowed, you must specify a policyTypes value that include \"Egress\" (since such a policy would not include an Egress section and would otherwise default to just [ \"Ingress\" ]). This field is beta-level in 1.8
    * @member {Array.<String>} policyTypes
    */
    policyTypes = undefined;








}


